// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/gitbundle/store/models/hooktask"
	"github.com/gitbundle/gitbundle/store/models/predicate"
)

// HookTaskQuery is the builder for querying HookTask entities.
type HookTaskQuery struct {
	config
	ctx        *QueryContext
	order      []hooktask.OrderOption
	inters     []Interceptor
	predicates []predicate.HookTask
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HookTaskQuery builder.
func (htq *HookTaskQuery) Where(ps ...predicate.HookTask) *HookTaskQuery {
	htq.predicates = append(htq.predicates, ps...)
	return htq
}

// Limit the number of records to be returned by this query.
func (htq *HookTaskQuery) Limit(limit int) *HookTaskQuery {
	htq.ctx.Limit = &limit
	return htq
}

// Offset to start from.
func (htq *HookTaskQuery) Offset(offset int) *HookTaskQuery {
	htq.ctx.Offset = &offset
	return htq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (htq *HookTaskQuery) Unique(unique bool) *HookTaskQuery {
	htq.ctx.Unique = &unique
	return htq
}

// Order specifies how the records should be ordered.
func (htq *HookTaskQuery) Order(o ...hooktask.OrderOption) *HookTaskQuery {
	htq.order = append(htq.order, o...)
	return htq
}

// First returns the first HookTask entity from the query.
// Returns a *NotFoundError when no HookTask was found.
func (htq *HookTaskQuery) First(ctx context.Context) (*HookTask, error) {
	nodes, err := htq.Limit(1).All(setContextOp(ctx, htq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{hooktask.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (htq *HookTaskQuery) FirstX(ctx context.Context) *HookTask {
	node, err := htq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first HookTask ID from the query.
// Returns a *NotFoundError when no HookTask ID was found.
func (htq *HookTaskQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = htq.Limit(1).IDs(setContextOp(ctx, htq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{hooktask.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (htq *HookTaskQuery) FirstIDX(ctx context.Context) int {
	id, err := htq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single HookTask entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one HookTask entity is found.
// Returns a *NotFoundError when no HookTask entities are found.
func (htq *HookTaskQuery) Only(ctx context.Context) (*HookTask, error) {
	nodes, err := htq.Limit(2).All(setContextOp(ctx, htq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{hooktask.Label}
	default:
		return nil, &NotSingularError{hooktask.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (htq *HookTaskQuery) OnlyX(ctx context.Context) *HookTask {
	node, err := htq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only HookTask ID in the query.
// Returns a *NotSingularError when more than one HookTask ID is found.
// Returns a *NotFoundError when no entities are found.
func (htq *HookTaskQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = htq.Limit(2).IDs(setContextOp(ctx, htq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{hooktask.Label}
	default:
		err = &NotSingularError{hooktask.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (htq *HookTaskQuery) OnlyIDX(ctx context.Context) int {
	id, err := htq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of HookTasks.
func (htq *HookTaskQuery) All(ctx context.Context) ([]*HookTask, error) {
	ctx = setContextOp(ctx, htq.ctx, "All")
	if err := htq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*HookTask, *HookTaskQuery]()
	return withInterceptors[[]*HookTask](ctx, htq, qr, htq.inters)
}

// AllX is like All, but panics if an error occurs.
func (htq *HookTaskQuery) AllX(ctx context.Context) []*HookTask {
	nodes, err := htq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of HookTask IDs.
func (htq *HookTaskQuery) IDs(ctx context.Context) (ids []int, err error) {
	if htq.ctx.Unique == nil && htq.path != nil {
		htq.Unique(true)
	}
	ctx = setContextOp(ctx, htq.ctx, "IDs")
	if err = htq.Select(hooktask.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (htq *HookTaskQuery) IDsX(ctx context.Context) []int {
	ids, err := htq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (htq *HookTaskQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, htq.ctx, "Count")
	if err := htq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, htq, querierCount[*HookTaskQuery](), htq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (htq *HookTaskQuery) CountX(ctx context.Context) int {
	count, err := htq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (htq *HookTaskQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, htq.ctx, "Exist")
	switch _, err := htq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (htq *HookTaskQuery) ExistX(ctx context.Context) bool {
	exist, err := htq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HookTaskQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (htq *HookTaskQuery) Clone() *HookTaskQuery {
	if htq == nil {
		return nil
	}
	return &HookTaskQuery{
		config:     htq.config,
		ctx:        htq.ctx.Clone(),
		order:      append([]hooktask.OrderOption{}, htq.order...),
		inters:     append([]Interceptor{}, htq.inters...),
		predicates: append([]predicate.HookTask{}, htq.predicates...),
		// clone intermediate query.
		sql:  htq.sql.Clone(),
		path: htq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RepoID int64 `json:"repo_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.HookTask.Query().
//		GroupBy(hooktask.FieldRepoID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (htq *HookTaskQuery) GroupBy(field string, fields ...string) *HookTaskGroupBy {
	htq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HookTaskGroupBy{build: htq}
	grbuild.flds = &htq.ctx.Fields
	grbuild.label = hooktask.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RepoID int64 `json:"repo_id,omitempty"`
//	}
//
//	client.HookTask.Query().
//		Select(hooktask.FieldRepoID).
//		Scan(ctx, &v)
func (htq *HookTaskQuery) Select(fields ...string) *HookTaskSelect {
	htq.ctx.Fields = append(htq.ctx.Fields, fields...)
	sbuild := &HookTaskSelect{HookTaskQuery: htq}
	sbuild.label = hooktask.Label
	sbuild.flds, sbuild.scan = &htq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HookTaskSelect configured with the given aggregations.
func (htq *HookTaskQuery) Aggregate(fns ...AggregateFunc) *HookTaskSelect {
	return htq.Select().Aggregate(fns...)
}

func (htq *HookTaskQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range htq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, htq); err != nil {
				return err
			}
		}
	}
	for _, f := range htq.ctx.Fields {
		if !hooktask.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if htq.path != nil {
		prev, err := htq.path(ctx)
		if err != nil {
			return err
		}
		htq.sql = prev
	}
	return nil
}

func (htq *HookTaskQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*HookTask, error) {
	var (
		nodes = []*HookTask{}
		_spec = htq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*HookTask).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &HookTask{config: htq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, htq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (htq *HookTaskQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := htq.querySpec()
	_spec.Node.Columns = htq.ctx.Fields
	if len(htq.ctx.Fields) > 0 {
		_spec.Unique = htq.ctx.Unique != nil && *htq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, htq.driver, _spec)
}

func (htq *HookTaskQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(hooktask.Table, hooktask.Columns, sqlgraph.NewFieldSpec(hooktask.FieldID, field.TypeInt))
	_spec.From = htq.sql
	if unique := htq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if htq.path != nil {
		_spec.Unique = true
	}
	if fields := htq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, hooktask.FieldID)
		for i := range fields {
			if fields[i] != hooktask.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := htq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := htq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := htq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := htq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (htq *HookTaskQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(htq.driver.Dialect())
	t1 := builder.Table(hooktask.Table)
	columns := htq.ctx.Fields
	if len(columns) == 0 {
		columns = hooktask.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if htq.sql != nil {
		selector = htq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if htq.ctx.Unique != nil && *htq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range htq.predicates {
		p(selector)
	}
	for _, p := range htq.order {
		p(selector)
	}
	if offset := htq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := htq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HookTaskGroupBy is the group-by builder for HookTask entities.
type HookTaskGroupBy struct {
	selector
	build *HookTaskQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (htgb *HookTaskGroupBy) Aggregate(fns ...AggregateFunc) *HookTaskGroupBy {
	htgb.fns = append(htgb.fns, fns...)
	return htgb
}

// Scan applies the selector query and scans the result into the given value.
func (htgb *HookTaskGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, htgb.build.ctx, "GroupBy")
	if err := htgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HookTaskQuery, *HookTaskGroupBy](ctx, htgb.build, htgb, htgb.build.inters, v)
}

func (htgb *HookTaskGroupBy) sqlScan(ctx context.Context, root *HookTaskQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(htgb.fns))
	for _, fn := range htgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*htgb.flds)+len(htgb.fns))
		for _, f := range *htgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*htgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := htgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HookTaskSelect is the builder for selecting fields of HookTask entities.
type HookTaskSelect struct {
	*HookTaskQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (hts *HookTaskSelect) Aggregate(fns ...AggregateFunc) *HookTaskSelect {
	hts.fns = append(hts.fns, fns...)
	return hts
}

// Scan applies the selector query and scans the result into the given value.
func (hts *HookTaskSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hts.ctx, "Select")
	if err := hts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HookTaskQuery, *HookTaskSelect](ctx, hts.HookTaskQuery, hts, hts.inters, v)
}

func (hts *HookTaskSelect) sqlScan(ctx context.Context, root *HookTaskQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(hts.fns))
	for _, fn := range hts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*hts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
