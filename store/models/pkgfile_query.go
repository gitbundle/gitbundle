// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/gitbundle/store/models/pkgfile"
	"github.com/gitbundle/gitbundle/store/models/predicate"
)

// PkgFileQuery is the builder for querying PkgFile entities.
type PkgFileQuery struct {
	config
	ctx        *QueryContext
	order      []pkgfile.OrderOption
	inters     []Interceptor
	predicates []predicate.PkgFile
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PkgFileQuery builder.
func (pfq *PkgFileQuery) Where(ps ...predicate.PkgFile) *PkgFileQuery {
	pfq.predicates = append(pfq.predicates, ps...)
	return pfq
}

// Limit the number of records to be returned by this query.
func (pfq *PkgFileQuery) Limit(limit int) *PkgFileQuery {
	pfq.ctx.Limit = &limit
	return pfq
}

// Offset to start from.
func (pfq *PkgFileQuery) Offset(offset int) *PkgFileQuery {
	pfq.ctx.Offset = &offset
	return pfq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pfq *PkgFileQuery) Unique(unique bool) *PkgFileQuery {
	pfq.ctx.Unique = &unique
	return pfq
}

// Order specifies how the records should be ordered.
func (pfq *PkgFileQuery) Order(o ...pkgfile.OrderOption) *PkgFileQuery {
	pfq.order = append(pfq.order, o...)
	return pfq
}

// First returns the first PkgFile entity from the query.
// Returns a *NotFoundError when no PkgFile was found.
func (pfq *PkgFileQuery) First(ctx context.Context) (*PkgFile, error) {
	nodes, err := pfq.Limit(1).All(setContextOp(ctx, pfq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{pkgfile.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pfq *PkgFileQuery) FirstX(ctx context.Context) *PkgFile {
	node, err := pfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PkgFile ID from the query.
// Returns a *NotFoundError when no PkgFile ID was found.
func (pfq *PkgFileQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pfq.Limit(1).IDs(setContextOp(ctx, pfq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{pkgfile.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pfq *PkgFileQuery) FirstIDX(ctx context.Context) int {
	id, err := pfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PkgFile entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PkgFile entity is found.
// Returns a *NotFoundError when no PkgFile entities are found.
func (pfq *PkgFileQuery) Only(ctx context.Context) (*PkgFile, error) {
	nodes, err := pfq.Limit(2).All(setContextOp(ctx, pfq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{pkgfile.Label}
	default:
		return nil, &NotSingularError{pkgfile.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pfq *PkgFileQuery) OnlyX(ctx context.Context) *PkgFile {
	node, err := pfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PkgFile ID in the query.
// Returns a *NotSingularError when more than one PkgFile ID is found.
// Returns a *NotFoundError when no entities are found.
func (pfq *PkgFileQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pfq.Limit(2).IDs(setContextOp(ctx, pfq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{pkgfile.Label}
	default:
		err = &NotSingularError{pkgfile.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pfq *PkgFileQuery) OnlyIDX(ctx context.Context) int {
	id, err := pfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PkgFiles.
func (pfq *PkgFileQuery) All(ctx context.Context) ([]*PkgFile, error) {
	ctx = setContextOp(ctx, pfq.ctx, "All")
	if err := pfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PkgFile, *PkgFileQuery]()
	return withInterceptors[[]*PkgFile](ctx, pfq, qr, pfq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pfq *PkgFileQuery) AllX(ctx context.Context) []*PkgFile {
	nodes, err := pfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PkgFile IDs.
func (pfq *PkgFileQuery) IDs(ctx context.Context) (ids []int, err error) {
	if pfq.ctx.Unique == nil && pfq.path != nil {
		pfq.Unique(true)
	}
	ctx = setContextOp(ctx, pfq.ctx, "IDs")
	if err = pfq.Select(pkgfile.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pfq *PkgFileQuery) IDsX(ctx context.Context) []int {
	ids, err := pfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pfq *PkgFileQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pfq.ctx, "Count")
	if err := pfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pfq, querierCount[*PkgFileQuery](), pfq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pfq *PkgFileQuery) CountX(ctx context.Context) int {
	count, err := pfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pfq *PkgFileQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pfq.ctx, "Exist")
	switch _, err := pfq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pfq *PkgFileQuery) ExistX(ctx context.Context) bool {
	exist, err := pfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PkgFileQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pfq *PkgFileQuery) Clone() *PkgFileQuery {
	if pfq == nil {
		return nil
	}
	return &PkgFileQuery{
		config:     pfq.config,
		ctx:        pfq.ctx.Clone(),
		order:      append([]pkgfile.OrderOption{}, pfq.order...),
		inters:     append([]Interceptor{}, pfq.inters...),
		predicates: append([]predicate.PkgFile{}, pfq.predicates...),
		// clone intermediate query.
		sql:  pfq.sql.Clone(),
		path: pfq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		VersionID int64 `json:"version_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PkgFile.Query().
//		GroupBy(pkgfile.FieldVersionID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (pfq *PkgFileQuery) GroupBy(field string, fields ...string) *PkgFileGroupBy {
	pfq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PkgFileGroupBy{build: pfq}
	grbuild.flds = &pfq.ctx.Fields
	grbuild.label = pkgfile.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		VersionID int64 `json:"version_id,omitempty"`
//	}
//
//	client.PkgFile.Query().
//		Select(pkgfile.FieldVersionID).
//		Scan(ctx, &v)
func (pfq *PkgFileQuery) Select(fields ...string) *PkgFileSelect {
	pfq.ctx.Fields = append(pfq.ctx.Fields, fields...)
	sbuild := &PkgFileSelect{PkgFileQuery: pfq}
	sbuild.label = pkgfile.Label
	sbuild.flds, sbuild.scan = &pfq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PkgFileSelect configured with the given aggregations.
func (pfq *PkgFileQuery) Aggregate(fns ...AggregateFunc) *PkgFileSelect {
	return pfq.Select().Aggregate(fns...)
}

func (pfq *PkgFileQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pfq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pfq); err != nil {
				return err
			}
		}
	}
	for _, f := range pfq.ctx.Fields {
		if !pkgfile.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if pfq.path != nil {
		prev, err := pfq.path(ctx)
		if err != nil {
			return err
		}
		pfq.sql = prev
	}
	return nil
}

func (pfq *PkgFileQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PkgFile, error) {
	var (
		nodes = []*PkgFile{}
		_spec = pfq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PkgFile).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PkgFile{config: pfq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (pfq *PkgFileQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pfq.querySpec()
	_spec.Node.Columns = pfq.ctx.Fields
	if len(pfq.ctx.Fields) > 0 {
		_spec.Unique = pfq.ctx.Unique != nil && *pfq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pfq.driver, _spec)
}

func (pfq *PkgFileQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(pkgfile.Table, pkgfile.Columns, sqlgraph.NewFieldSpec(pkgfile.FieldID, field.TypeInt))
	_spec.From = pfq.sql
	if unique := pfq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pfq.path != nil {
		_spec.Unique = true
	}
	if fields := pfq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, pkgfile.FieldID)
		for i := range fields {
			if fields[i] != pkgfile.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pfq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pfq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pfq *PkgFileQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pfq.driver.Dialect())
	t1 := builder.Table(pkgfile.Table)
	columns := pfq.ctx.Fields
	if len(columns) == 0 {
		columns = pkgfile.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pfq.sql != nil {
		selector = pfq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pfq.ctx.Unique != nil && *pfq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pfq.predicates {
		p(selector)
	}
	for _, p := range pfq.order {
		p(selector)
	}
	if offset := pfq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pfq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PkgFileGroupBy is the group-by builder for PkgFile entities.
type PkgFileGroupBy struct {
	selector
	build *PkgFileQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pfgb *PkgFileGroupBy) Aggregate(fns ...AggregateFunc) *PkgFileGroupBy {
	pfgb.fns = append(pfgb.fns, fns...)
	return pfgb
}

// Scan applies the selector query and scans the result into the given value.
func (pfgb *PkgFileGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pfgb.build.ctx, "GroupBy")
	if err := pfgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PkgFileQuery, *PkgFileGroupBy](ctx, pfgb.build, pfgb, pfgb.build.inters, v)
}

func (pfgb *PkgFileGroupBy) sqlScan(ctx context.Context, root *PkgFileQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pfgb.fns))
	for _, fn := range pfgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pfgb.flds)+len(pfgb.fns))
		for _, f := range *pfgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pfgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pfgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PkgFileSelect is the builder for selecting fields of PkgFile entities.
type PkgFileSelect struct {
	*PkgFileQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pfs *PkgFileSelect) Aggregate(fns ...AggregateFunc) *PkgFileSelect {
	pfs.fns = append(pfs.fns, fns...)
	return pfs
}

// Scan applies the selector query and scans the result into the given value.
func (pfs *PkgFileSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pfs.ctx, "Select")
	if err := pfs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PkgFileQuery, *PkgFileSelect](ctx, pfs.PkgFileQuery, pfs, pfs.inters, v)
}

func (pfs *PkgFileSelect) sqlScan(ctx context.Context, root *PkgFileQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pfs.fns))
	for _, fn := range pfs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pfs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
