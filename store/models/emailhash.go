// Code generated by ent, DO NOT EDIT.

package models

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/gitbundle/gitbundle/store/models/emailhash"
)

// EmailHash is the model entity for the EmailHash schema.
type EmailHash struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Hash holds the value of the "hash" field.
	Hash string `json:"hash,omitempty"`
	// Email holds the value of the "email" field.
	Email        string `json:"email,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EmailHash) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case emailhash.FieldID:
			values[i] = new(sql.NullInt64)
		case emailhash.FieldHash, emailhash.FieldEmail:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EmailHash fields.
func (eh *EmailHash) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case emailhash.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			eh.ID = int(value.Int64)
		case emailhash.FieldHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field hash", values[i])
			} else if value.Valid {
				eh.Hash = value.String
			}
		case emailhash.FieldEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field email", values[i])
			} else if value.Valid {
				eh.Email = value.String
			}
		default:
			eh.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the EmailHash.
// This includes values selected through modifiers, order, etc.
func (eh *EmailHash) Value(name string) (ent.Value, error) {
	return eh.selectValues.Get(name)
}

// Update returns a builder for updating this EmailHash.
// Note that you need to call EmailHash.Unwrap() before calling this method if this EmailHash
// was returned from a transaction, and the transaction was committed or rolled back.
func (eh *EmailHash) Update() *EmailHashUpdateOne {
	return NewEmailHashClient(eh.config).UpdateOne(eh)
}

// Unwrap unwraps the EmailHash entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (eh *EmailHash) Unwrap() *EmailHash {
	_tx, ok := eh.config.driver.(*txDriver)
	if !ok {
		panic("models: EmailHash is not a transactional entity")
	}
	eh.config.driver = _tx.drv
	return eh
}

// String implements the fmt.Stringer.
func (eh *EmailHash) String() string {
	var builder strings.Builder
	builder.WriteString("EmailHash(")
	builder.WriteString(fmt.Sprintf("id=%v, ", eh.ID))
	builder.WriteString("hash=")
	builder.WriteString(eh.Hash)
	builder.WriteString(", ")
	builder.WriteString("email=")
	builder.WriteString(eh.Email)
	builder.WriteByte(')')
	return builder.String()
}

// EmailHashes is a parsable slice of EmailHash.
type EmailHashes []*EmailHash
