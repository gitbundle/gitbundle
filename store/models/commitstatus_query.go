// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/gitbundle/store/models/commitstatus"
	"github.com/gitbundle/gitbundle/store/models/predicate"
)

// CommitStatusQuery is the builder for querying CommitStatus entities.
type CommitStatusQuery struct {
	config
	ctx        *QueryContext
	order      []commitstatus.OrderOption
	inters     []Interceptor
	predicates []predicate.CommitStatus
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CommitStatusQuery builder.
func (csq *CommitStatusQuery) Where(ps ...predicate.CommitStatus) *CommitStatusQuery {
	csq.predicates = append(csq.predicates, ps...)
	return csq
}

// Limit the number of records to be returned by this query.
func (csq *CommitStatusQuery) Limit(limit int) *CommitStatusQuery {
	csq.ctx.Limit = &limit
	return csq
}

// Offset to start from.
func (csq *CommitStatusQuery) Offset(offset int) *CommitStatusQuery {
	csq.ctx.Offset = &offset
	return csq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (csq *CommitStatusQuery) Unique(unique bool) *CommitStatusQuery {
	csq.ctx.Unique = &unique
	return csq
}

// Order specifies how the records should be ordered.
func (csq *CommitStatusQuery) Order(o ...commitstatus.OrderOption) *CommitStatusQuery {
	csq.order = append(csq.order, o...)
	return csq
}

// First returns the first CommitStatus entity from the query.
// Returns a *NotFoundError when no CommitStatus was found.
func (csq *CommitStatusQuery) First(ctx context.Context) (*CommitStatus, error) {
	nodes, err := csq.Limit(1).All(setContextOp(ctx, csq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{commitstatus.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (csq *CommitStatusQuery) FirstX(ctx context.Context) *CommitStatus {
	node, err := csq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CommitStatus ID from the query.
// Returns a *NotFoundError when no CommitStatus ID was found.
func (csq *CommitStatusQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = csq.Limit(1).IDs(setContextOp(ctx, csq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{commitstatus.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (csq *CommitStatusQuery) FirstIDX(ctx context.Context) int {
	id, err := csq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CommitStatus entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CommitStatus entity is found.
// Returns a *NotFoundError when no CommitStatus entities are found.
func (csq *CommitStatusQuery) Only(ctx context.Context) (*CommitStatus, error) {
	nodes, err := csq.Limit(2).All(setContextOp(ctx, csq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{commitstatus.Label}
	default:
		return nil, &NotSingularError{commitstatus.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (csq *CommitStatusQuery) OnlyX(ctx context.Context) *CommitStatus {
	node, err := csq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CommitStatus ID in the query.
// Returns a *NotSingularError when more than one CommitStatus ID is found.
// Returns a *NotFoundError when no entities are found.
func (csq *CommitStatusQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = csq.Limit(2).IDs(setContextOp(ctx, csq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{commitstatus.Label}
	default:
		err = &NotSingularError{commitstatus.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (csq *CommitStatusQuery) OnlyIDX(ctx context.Context) int {
	id, err := csq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CommitStatusSlice.
func (csq *CommitStatusQuery) All(ctx context.Context) ([]*CommitStatus, error) {
	ctx = setContextOp(ctx, csq.ctx, "All")
	if err := csq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CommitStatus, *CommitStatusQuery]()
	return withInterceptors[[]*CommitStatus](ctx, csq, qr, csq.inters)
}

// AllX is like All, but panics if an error occurs.
func (csq *CommitStatusQuery) AllX(ctx context.Context) []*CommitStatus {
	nodes, err := csq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CommitStatus IDs.
func (csq *CommitStatusQuery) IDs(ctx context.Context) (ids []int, err error) {
	if csq.ctx.Unique == nil && csq.path != nil {
		csq.Unique(true)
	}
	ctx = setContextOp(ctx, csq.ctx, "IDs")
	if err = csq.Select(commitstatus.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (csq *CommitStatusQuery) IDsX(ctx context.Context) []int {
	ids, err := csq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (csq *CommitStatusQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, csq.ctx, "Count")
	if err := csq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, csq, querierCount[*CommitStatusQuery](), csq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (csq *CommitStatusQuery) CountX(ctx context.Context) int {
	count, err := csq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (csq *CommitStatusQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, csq.ctx, "Exist")
	switch _, err := csq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (csq *CommitStatusQuery) ExistX(ctx context.Context) bool {
	exist, err := csq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CommitStatusQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (csq *CommitStatusQuery) Clone() *CommitStatusQuery {
	if csq == nil {
		return nil
	}
	return &CommitStatusQuery{
		config:     csq.config,
		ctx:        csq.ctx.Clone(),
		order:      append([]commitstatus.OrderOption{}, csq.order...),
		inters:     append([]Interceptor{}, csq.inters...),
		predicates: append([]predicate.CommitStatus{}, csq.predicates...),
		// clone intermediate query.
		sql:  csq.sql.Clone(),
		path: csq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Index int64 `json:"index,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CommitStatus.Query().
//		GroupBy(commitstatus.FieldIndex).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (csq *CommitStatusQuery) GroupBy(field string, fields ...string) *CommitStatusGroupBy {
	csq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CommitStatusGroupBy{build: csq}
	grbuild.flds = &csq.ctx.Fields
	grbuild.label = commitstatus.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Index int64 `json:"index,omitempty"`
//	}
//
//	client.CommitStatus.Query().
//		Select(commitstatus.FieldIndex).
//		Scan(ctx, &v)
func (csq *CommitStatusQuery) Select(fields ...string) *CommitStatusSelect {
	csq.ctx.Fields = append(csq.ctx.Fields, fields...)
	sbuild := &CommitStatusSelect{CommitStatusQuery: csq}
	sbuild.label = commitstatus.Label
	sbuild.flds, sbuild.scan = &csq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CommitStatusSelect configured with the given aggregations.
func (csq *CommitStatusQuery) Aggregate(fns ...AggregateFunc) *CommitStatusSelect {
	return csq.Select().Aggregate(fns...)
}

func (csq *CommitStatusQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range csq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, csq); err != nil {
				return err
			}
		}
	}
	for _, f := range csq.ctx.Fields {
		if !commitstatus.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if csq.path != nil {
		prev, err := csq.path(ctx)
		if err != nil {
			return err
		}
		csq.sql = prev
	}
	return nil
}

func (csq *CommitStatusQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CommitStatus, error) {
	var (
		nodes = []*CommitStatus{}
		_spec = csq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CommitStatus).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CommitStatus{config: csq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, csq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (csq *CommitStatusQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := csq.querySpec()
	_spec.Node.Columns = csq.ctx.Fields
	if len(csq.ctx.Fields) > 0 {
		_spec.Unique = csq.ctx.Unique != nil && *csq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, csq.driver, _spec)
}

func (csq *CommitStatusQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(commitstatus.Table, commitstatus.Columns, sqlgraph.NewFieldSpec(commitstatus.FieldID, field.TypeInt))
	_spec.From = csq.sql
	if unique := csq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if csq.path != nil {
		_spec.Unique = true
	}
	if fields := csq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, commitstatus.FieldID)
		for i := range fields {
			if fields[i] != commitstatus.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := csq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := csq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := csq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := csq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (csq *CommitStatusQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(csq.driver.Dialect())
	t1 := builder.Table(commitstatus.Table)
	columns := csq.ctx.Fields
	if len(columns) == 0 {
		columns = commitstatus.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if csq.sql != nil {
		selector = csq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if csq.ctx.Unique != nil && *csq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range csq.predicates {
		p(selector)
	}
	for _, p := range csq.order {
		p(selector)
	}
	if offset := csq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := csq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CommitStatusGroupBy is the group-by builder for CommitStatus entities.
type CommitStatusGroupBy struct {
	selector
	build *CommitStatusQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (csgb *CommitStatusGroupBy) Aggregate(fns ...AggregateFunc) *CommitStatusGroupBy {
	csgb.fns = append(csgb.fns, fns...)
	return csgb
}

// Scan applies the selector query and scans the result into the given value.
func (csgb *CommitStatusGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, csgb.build.ctx, "GroupBy")
	if err := csgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CommitStatusQuery, *CommitStatusGroupBy](ctx, csgb.build, csgb, csgb.build.inters, v)
}

func (csgb *CommitStatusGroupBy) sqlScan(ctx context.Context, root *CommitStatusQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(csgb.fns))
	for _, fn := range csgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*csgb.flds)+len(csgb.fns))
		for _, f := range *csgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*csgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := csgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CommitStatusSelect is the builder for selecting fields of CommitStatus entities.
type CommitStatusSelect struct {
	*CommitStatusQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (css *CommitStatusSelect) Aggregate(fns ...AggregateFunc) *CommitStatusSelect {
	css.fns = append(css.fns, fns...)
	return css
}

// Scan applies the selector query and scans the result into the given value.
func (css *CommitStatusSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, css.ctx, "Select")
	if err := css.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CommitStatusQuery, *CommitStatusSelect](ctx, css.CommitStatusQuery, css, css.inters, v)
}

func (css *CommitStatusSelect) sqlScan(ctx context.Context, root *CommitStatusQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(css.fns))
	for _, fn := range css.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*css.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := css.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
