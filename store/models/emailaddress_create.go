// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/server/store/models/emailaddress"
)

// EmailAddressCreate is the builder for creating a EmailAddress entity.
type EmailAddressCreate struct {
	config
	mutation *EmailAddressMutation
	hooks    []Hook
}

// SetUserID sets the "user_id" field.
func (eac *EmailAddressCreate) SetUserID(i int64) *EmailAddressCreate {
	eac.mutation.SetUserID(i)
	return eac
}

// SetEmail sets the "email" field.
func (eac *EmailAddressCreate) SetEmail(s string) *EmailAddressCreate {
	eac.mutation.SetEmail(s)
	return eac
}

// SetLowerEmail sets the "lower_email" field.
func (eac *EmailAddressCreate) SetLowerEmail(s string) *EmailAddressCreate {
	eac.mutation.SetLowerEmail(s)
	return eac
}

// SetIsActivated sets the "is_activated" field.
func (eac *EmailAddressCreate) SetIsActivated(b bool) *EmailAddressCreate {
	eac.mutation.SetIsActivated(b)
	return eac
}

// SetNillableIsActivated sets the "is_activated" field if the given value is not nil.
func (eac *EmailAddressCreate) SetNillableIsActivated(b *bool) *EmailAddressCreate {
	if b != nil {
		eac.SetIsActivated(*b)
	}
	return eac
}

// SetIsPrimary sets the "is_primary" field.
func (eac *EmailAddressCreate) SetIsPrimary(b bool) *EmailAddressCreate {
	eac.mutation.SetIsPrimary(b)
	return eac
}

// SetNillableIsPrimary sets the "is_primary" field if the given value is not nil.
func (eac *EmailAddressCreate) SetNillableIsPrimary(b *bool) *EmailAddressCreate {
	if b != nil {
		eac.SetIsPrimary(*b)
	}
	return eac
}

// Mutation returns the EmailAddressMutation object of the builder.
func (eac *EmailAddressCreate) Mutation() *EmailAddressMutation {
	return eac.mutation
}

// Save creates the EmailAddress in the database.
func (eac *EmailAddressCreate) Save(ctx context.Context) (*EmailAddress, error) {
	eac.defaults()
	return withHooks(ctx, eac.sqlSave, eac.mutation, eac.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (eac *EmailAddressCreate) SaveX(ctx context.Context) *EmailAddress {
	v, err := eac.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (eac *EmailAddressCreate) Exec(ctx context.Context) error {
	_, err := eac.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (eac *EmailAddressCreate) ExecX(ctx context.Context) {
	if err := eac.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (eac *EmailAddressCreate) defaults() {
	if _, ok := eac.mutation.IsPrimary(); !ok {
		v := emailaddress.DefaultIsPrimary
		eac.mutation.SetIsPrimary(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (eac *EmailAddressCreate) check() error {
	if _, ok := eac.mutation.UserID(); !ok {
		return &ValidationError{Name: "user_id", err: errors.New(`models: missing required field "EmailAddress.user_id"`)}
	}
	if _, ok := eac.mutation.Email(); !ok {
		return &ValidationError{Name: "email", err: errors.New(`models: missing required field "EmailAddress.email"`)}
	}
	if v, ok := eac.mutation.Email(); ok {
		if err := emailaddress.EmailValidator(v); err != nil {
			return &ValidationError{Name: "email", err: fmt.Errorf(`models: validator failed for field "EmailAddress.email": %w`, err)}
		}
	}
	if _, ok := eac.mutation.LowerEmail(); !ok {
		return &ValidationError{Name: "lower_email", err: errors.New(`models: missing required field "EmailAddress.lower_email"`)}
	}
	if v, ok := eac.mutation.LowerEmail(); ok {
		if err := emailaddress.LowerEmailValidator(v); err != nil {
			return &ValidationError{Name: "lower_email", err: fmt.Errorf(`models: validator failed for field "EmailAddress.lower_email": %w`, err)}
		}
	}
	if _, ok := eac.mutation.IsPrimary(); !ok {
		return &ValidationError{Name: "is_primary", err: errors.New(`models: missing required field "EmailAddress.is_primary"`)}
	}
	return nil
}

func (eac *EmailAddressCreate) sqlSave(ctx context.Context) (*EmailAddress, error) {
	if err := eac.check(); err != nil {
		return nil, err
	}
	_node, _spec := eac.createSpec()
	if err := sqlgraph.CreateNode(ctx, eac.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	eac.mutation.id = &_node.ID
	eac.mutation.done = true
	return _node, nil
}

func (eac *EmailAddressCreate) createSpec() (*EmailAddress, *sqlgraph.CreateSpec) {
	var (
		_node = &EmailAddress{config: eac.config}
		_spec = sqlgraph.NewCreateSpec(emailaddress.Table, sqlgraph.NewFieldSpec(emailaddress.FieldID, field.TypeInt))
	)
	if value, ok := eac.mutation.UserID(); ok {
		_spec.SetField(emailaddress.FieldUserID, field.TypeInt64, value)
		_node.UserID = value
	}
	if value, ok := eac.mutation.Email(); ok {
		_spec.SetField(emailaddress.FieldEmail, field.TypeString, value)
		_node.Email = value
	}
	if value, ok := eac.mutation.LowerEmail(); ok {
		_spec.SetField(emailaddress.FieldLowerEmail, field.TypeString, value)
		_node.LowerEmail = value
	}
	if value, ok := eac.mutation.IsActivated(); ok {
		_spec.SetField(emailaddress.FieldIsActivated, field.TypeBool, value)
		_node.IsActivated = value
	}
	if value, ok := eac.mutation.IsPrimary(); ok {
		_spec.SetField(emailaddress.FieldIsPrimary, field.TypeBool, value)
		_node.IsPrimary = value
	}
	return _node, _spec
}

// EmailAddressCreateBulk is the builder for creating many EmailAddress entities in bulk.
type EmailAddressCreateBulk struct {
	config
	err      error
	builders []*EmailAddressCreate
}

// Save creates the EmailAddress entities in the database.
func (eacb *EmailAddressCreateBulk) Save(ctx context.Context) ([]*EmailAddress, error) {
	if eacb.err != nil {
		return nil, eacb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(eacb.builders))
	nodes := make([]*EmailAddress, len(eacb.builders))
	mutators := make([]Mutator, len(eacb.builders))
	for i := range eacb.builders {
		func(i int, root context.Context) {
			builder := eacb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*EmailAddressMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, eacb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, eacb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, eacb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (eacb *EmailAddressCreateBulk) SaveX(ctx context.Context) []*EmailAddress {
	v, err := eacb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (eacb *EmailAddressCreateBulk) Exec(ctx context.Context) error {
	_, err := eacb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (eacb *EmailAddressCreateBulk) ExecX(ctx context.Context) {
	if err := eacb.Exec(ctx); err != nil {
		panic(err)
	}
}
