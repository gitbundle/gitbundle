// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/gitbundle/store/models/useropenid"
)

// UserOpenidCreate is the builder for creating a UserOpenid entity.
type UserOpenidCreate struct {
	config
	mutation *UserOpenidMutation
	hooks    []Hook
}

// SetUserID sets the "user_id" field.
func (uoc *UserOpenidCreate) SetUserID(i int64) *UserOpenidCreate {
	uoc.mutation.SetUserID(i)
	return uoc
}

// SetNillableUserID sets the "user_id" field if the given value is not nil.
func (uoc *UserOpenidCreate) SetNillableUserID(i *int64) *UserOpenidCreate {
	if i != nil {
		uoc.SetUserID(*i)
	}
	return uoc
}

// SetURI sets the "uri" field.
func (uoc *UserOpenidCreate) SetURI(s string) *UserOpenidCreate {
	uoc.mutation.SetURI(s)
	return uoc
}

// SetNillableURI sets the "uri" field if the given value is not nil.
func (uoc *UserOpenidCreate) SetNillableURI(s *string) *UserOpenidCreate {
	if s != nil {
		uoc.SetURI(*s)
	}
	return uoc
}

// SetShow sets the "show" field.
func (uoc *UserOpenidCreate) SetShow(b bool) *UserOpenidCreate {
	uoc.mutation.SetShow(b)
	return uoc
}

// SetNillableShow sets the "show" field if the given value is not nil.
func (uoc *UserOpenidCreate) SetNillableShow(b *bool) *UserOpenidCreate {
	if b != nil {
		uoc.SetShow(*b)
	}
	return uoc
}

// Mutation returns the UserOpenidMutation object of the builder.
func (uoc *UserOpenidCreate) Mutation() *UserOpenidMutation {
	return uoc.mutation
}

// Save creates the UserOpenid in the database.
func (uoc *UserOpenidCreate) Save(ctx context.Context) (*UserOpenid, error) {
	uoc.defaults()
	return withHooks(ctx, uoc.sqlSave, uoc.mutation, uoc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (uoc *UserOpenidCreate) SaveX(ctx context.Context) *UserOpenid {
	v, err := uoc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (uoc *UserOpenidCreate) Exec(ctx context.Context) error {
	_, err := uoc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (uoc *UserOpenidCreate) ExecX(ctx context.Context) {
	if err := uoc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (uoc *UserOpenidCreate) defaults() {
	if _, ok := uoc.mutation.Show(); !ok {
		v := useropenid.DefaultShow
		uoc.mutation.SetShow(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (uoc *UserOpenidCreate) check() error {
	if v, ok := uoc.mutation.URI(); ok {
		if err := useropenid.URIValidator(v); err != nil {
			return &ValidationError{Name: "uri", err: fmt.Errorf(`models: validator failed for field "UserOpenid.uri": %w`, err)}
		}
	}
	if _, ok := uoc.mutation.Show(); !ok {
		return &ValidationError{Name: "show", err: errors.New(`models: missing required field "UserOpenid.show"`)}
	}
	return nil
}

func (uoc *UserOpenidCreate) sqlSave(ctx context.Context) (*UserOpenid, error) {
	if err := uoc.check(); err != nil {
		return nil, err
	}
	_node, _spec := uoc.createSpec()
	if err := sqlgraph.CreateNode(ctx, uoc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	uoc.mutation.id = &_node.ID
	uoc.mutation.done = true
	return _node, nil
}

func (uoc *UserOpenidCreate) createSpec() (*UserOpenid, *sqlgraph.CreateSpec) {
	var (
		_node = &UserOpenid{config: uoc.config}
		_spec = sqlgraph.NewCreateSpec(useropenid.Table, sqlgraph.NewFieldSpec(useropenid.FieldID, field.TypeInt))
	)
	if value, ok := uoc.mutation.UserID(); ok {
		_spec.SetField(useropenid.FieldUserID, field.TypeInt64, value)
		_node.UserID = value
	}
	if value, ok := uoc.mutation.URI(); ok {
		_spec.SetField(useropenid.FieldURI, field.TypeString, value)
		_node.URI = value
	}
	if value, ok := uoc.mutation.Show(); ok {
		_spec.SetField(useropenid.FieldShow, field.TypeBool, value)
		_node.Show = value
	}
	return _node, _spec
}

// UserOpenidCreateBulk is the builder for creating many UserOpenid entities in bulk.
type UserOpenidCreateBulk struct {
	config
	err      error
	builders []*UserOpenidCreate
}

// Save creates the UserOpenid entities in the database.
func (uocb *UserOpenidCreateBulk) Save(ctx context.Context) ([]*UserOpenid, error) {
	if uocb.err != nil {
		return nil, uocb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(uocb.builders))
	nodes := make([]*UserOpenid, len(uocb.builders))
	mutators := make([]Mutator, len(uocb.builders))
	for i := range uocb.builders {
		func(i int, root context.Context) {
			builder := uocb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*UserOpenidMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, uocb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, uocb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, uocb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (uocb *UserOpenidCreateBulk) SaveX(ctx context.Context) []*UserOpenid {
	v, err := uocb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (uocb *UserOpenidCreateBulk) Exec(ctx context.Context) error {
	_, err := uocb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (uocb *UserOpenidCreateBulk) ExecX(ctx context.Context) {
	if err := uocb.Exec(ctx); err != nil {
		panic(err)
	}
}
