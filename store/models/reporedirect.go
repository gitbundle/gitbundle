// Code generated by ent, DO NOT EDIT.

package models

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/gitbundle/server/store/models/reporedirect"
)

// RepoRedirect is the model entity for the RepoRedirect schema.
type RepoRedirect struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// OwnerID holds the value of the "owner_id" field.
	OwnerID int64 `json:"owner_id,omitempty"`
	// LowerName holds the value of the "lower_name" field.
	LowerName string `json:"lower_name,omitempty"`
	// RedirectRepoID holds the value of the "redirect_repo_id" field.
	RedirectRepoID int64 `json:"redirect_repo_id,omitempty"`
	selectValues   sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RepoRedirect) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case reporedirect.FieldID, reporedirect.FieldOwnerID, reporedirect.FieldRedirectRepoID:
			values[i] = new(sql.NullInt64)
		case reporedirect.FieldLowerName:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RepoRedirect fields.
func (rr *RepoRedirect) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case reporedirect.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			rr.ID = int(value.Int64)
		case reporedirect.FieldOwnerID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field owner_id", values[i])
			} else if value.Valid {
				rr.OwnerID = value.Int64
			}
		case reporedirect.FieldLowerName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field lower_name", values[i])
			} else if value.Valid {
				rr.LowerName = value.String
			}
		case reporedirect.FieldRedirectRepoID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field redirect_repo_id", values[i])
			} else if value.Valid {
				rr.RedirectRepoID = value.Int64
			}
		default:
			rr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RepoRedirect.
// This includes values selected through modifiers, order, etc.
func (rr *RepoRedirect) Value(name string) (ent.Value, error) {
	return rr.selectValues.Get(name)
}

// Update returns a builder for updating this RepoRedirect.
// Note that you need to call RepoRedirect.Unwrap() before calling this method if this RepoRedirect
// was returned from a transaction, and the transaction was committed or rolled back.
func (rr *RepoRedirect) Update() *RepoRedirectUpdateOne {
	return NewRepoRedirectClient(rr.config).UpdateOne(rr)
}

// Unwrap unwraps the RepoRedirect entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rr *RepoRedirect) Unwrap() *RepoRedirect {
	_tx, ok := rr.config.driver.(*txDriver)
	if !ok {
		panic("models: RepoRedirect is not a transactional entity")
	}
	rr.config.driver = _tx.drv
	return rr
}

// String implements the fmt.Stringer.
func (rr *RepoRedirect) String() string {
	var builder strings.Builder
	builder.WriteString("RepoRedirect(")
	builder.WriteString(fmt.Sprintf("id=%v, ", rr.ID))
	builder.WriteString("owner_id=")
	builder.WriteString(fmt.Sprintf("%v", rr.OwnerID))
	builder.WriteString(", ")
	builder.WriteString("lower_name=")
	builder.WriteString(rr.LowerName)
	builder.WriteString(", ")
	builder.WriteString("redirect_repo_id=")
	builder.WriteString(fmt.Sprintf("%v", rr.RedirectRepoID))
	builder.WriteByte(')')
	return builder.String()
}

// RepoRedirects is a parsable slice of RepoRedirect.
type RepoRedirects []*RepoRedirect
