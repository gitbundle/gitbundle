// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/server/store/models/oauth2application"
	"github.com/gitbundle/server/store/models/predicate"
)

// Oauth2ApplicationQuery is the builder for querying Oauth2Application entities.
type Oauth2ApplicationQuery struct {
	config
	ctx        *QueryContext
	order      []oauth2application.OrderOption
	inters     []Interceptor
	predicates []predicate.Oauth2Application
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the Oauth2ApplicationQuery builder.
func (oq *Oauth2ApplicationQuery) Where(ps ...predicate.Oauth2Application) *Oauth2ApplicationQuery {
	oq.predicates = append(oq.predicates, ps...)
	return oq
}

// Limit the number of records to be returned by this query.
func (oq *Oauth2ApplicationQuery) Limit(limit int) *Oauth2ApplicationQuery {
	oq.ctx.Limit = &limit
	return oq
}

// Offset to start from.
func (oq *Oauth2ApplicationQuery) Offset(offset int) *Oauth2ApplicationQuery {
	oq.ctx.Offset = &offset
	return oq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (oq *Oauth2ApplicationQuery) Unique(unique bool) *Oauth2ApplicationQuery {
	oq.ctx.Unique = &unique
	return oq
}

// Order specifies how the records should be ordered.
func (oq *Oauth2ApplicationQuery) Order(o ...oauth2application.OrderOption) *Oauth2ApplicationQuery {
	oq.order = append(oq.order, o...)
	return oq
}

// First returns the first Oauth2Application entity from the query.
// Returns a *NotFoundError when no Oauth2Application was found.
func (oq *Oauth2ApplicationQuery) First(ctx context.Context) (*Oauth2Application, error) {
	nodes, err := oq.Limit(1).All(setContextOp(ctx, oq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{oauth2application.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (oq *Oauth2ApplicationQuery) FirstX(ctx context.Context) *Oauth2Application {
	node, err := oq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Oauth2Application ID from the query.
// Returns a *NotFoundError when no Oauth2Application ID was found.
func (oq *Oauth2ApplicationQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = oq.Limit(1).IDs(setContextOp(ctx, oq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{oauth2application.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (oq *Oauth2ApplicationQuery) FirstIDX(ctx context.Context) int {
	id, err := oq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Oauth2Application entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Oauth2Application entity is found.
// Returns a *NotFoundError when no Oauth2Application entities are found.
func (oq *Oauth2ApplicationQuery) Only(ctx context.Context) (*Oauth2Application, error) {
	nodes, err := oq.Limit(2).All(setContextOp(ctx, oq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{oauth2application.Label}
	default:
		return nil, &NotSingularError{oauth2application.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (oq *Oauth2ApplicationQuery) OnlyX(ctx context.Context) *Oauth2Application {
	node, err := oq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Oauth2Application ID in the query.
// Returns a *NotSingularError when more than one Oauth2Application ID is found.
// Returns a *NotFoundError when no entities are found.
func (oq *Oauth2ApplicationQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = oq.Limit(2).IDs(setContextOp(ctx, oq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{oauth2application.Label}
	default:
		err = &NotSingularError{oauth2application.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (oq *Oauth2ApplicationQuery) OnlyIDX(ctx context.Context) int {
	id, err := oq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Oauth2Applications.
func (oq *Oauth2ApplicationQuery) All(ctx context.Context) ([]*Oauth2Application, error) {
	ctx = setContextOp(ctx, oq.ctx, "All")
	if err := oq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Oauth2Application, *Oauth2ApplicationQuery]()
	return withInterceptors[[]*Oauth2Application](ctx, oq, qr, oq.inters)
}

// AllX is like All, but panics if an error occurs.
func (oq *Oauth2ApplicationQuery) AllX(ctx context.Context) []*Oauth2Application {
	nodes, err := oq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Oauth2Application IDs.
func (oq *Oauth2ApplicationQuery) IDs(ctx context.Context) (ids []int, err error) {
	if oq.ctx.Unique == nil && oq.path != nil {
		oq.Unique(true)
	}
	ctx = setContextOp(ctx, oq.ctx, "IDs")
	if err = oq.Select(oauth2application.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (oq *Oauth2ApplicationQuery) IDsX(ctx context.Context) []int {
	ids, err := oq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (oq *Oauth2ApplicationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, oq.ctx, "Count")
	if err := oq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, oq, querierCount[*Oauth2ApplicationQuery](), oq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (oq *Oauth2ApplicationQuery) CountX(ctx context.Context) int {
	count, err := oq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (oq *Oauth2ApplicationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, oq.ctx, "Exist")
	switch _, err := oq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (oq *Oauth2ApplicationQuery) ExistX(ctx context.Context) bool {
	exist, err := oq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the Oauth2ApplicationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (oq *Oauth2ApplicationQuery) Clone() *Oauth2ApplicationQuery {
	if oq == nil {
		return nil
	}
	return &Oauth2ApplicationQuery{
		config:     oq.config,
		ctx:        oq.ctx.Clone(),
		order:      append([]oauth2application.OrderOption{}, oq.order...),
		inters:     append([]Interceptor{}, oq.inters...),
		predicates: append([]predicate.Oauth2Application{}, oq.predicates...),
		// clone intermediate query.
		sql:  oq.sql.Clone(),
		path: oq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int64 `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Oauth2Application.Query().
//		GroupBy(oauth2application.FieldUserID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (oq *Oauth2ApplicationQuery) GroupBy(field string, fields ...string) *Oauth2ApplicationGroupBy {
	oq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &Oauth2ApplicationGroupBy{build: oq}
	grbuild.flds = &oq.ctx.Fields
	grbuild.label = oauth2application.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int64 `json:"user_id,omitempty"`
//	}
//
//	client.Oauth2Application.Query().
//		Select(oauth2application.FieldUserID).
//		Scan(ctx, &v)
func (oq *Oauth2ApplicationQuery) Select(fields ...string) *Oauth2ApplicationSelect {
	oq.ctx.Fields = append(oq.ctx.Fields, fields...)
	sbuild := &Oauth2ApplicationSelect{Oauth2ApplicationQuery: oq}
	sbuild.label = oauth2application.Label
	sbuild.flds, sbuild.scan = &oq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a Oauth2ApplicationSelect configured with the given aggregations.
func (oq *Oauth2ApplicationQuery) Aggregate(fns ...AggregateFunc) *Oauth2ApplicationSelect {
	return oq.Select().Aggregate(fns...)
}

func (oq *Oauth2ApplicationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range oq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, oq); err != nil {
				return err
			}
		}
	}
	for _, f := range oq.ctx.Fields {
		if !oauth2application.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if oq.path != nil {
		prev, err := oq.path(ctx)
		if err != nil {
			return err
		}
		oq.sql = prev
	}
	return nil
}

func (oq *Oauth2ApplicationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Oauth2Application, error) {
	var (
		nodes = []*Oauth2Application{}
		_spec = oq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Oauth2Application).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Oauth2Application{config: oq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, oq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (oq *Oauth2ApplicationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := oq.querySpec()
	_spec.Node.Columns = oq.ctx.Fields
	if len(oq.ctx.Fields) > 0 {
		_spec.Unique = oq.ctx.Unique != nil && *oq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, oq.driver, _spec)
}

func (oq *Oauth2ApplicationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(oauth2application.Table, oauth2application.Columns, sqlgraph.NewFieldSpec(oauth2application.FieldID, field.TypeInt))
	_spec.From = oq.sql
	if unique := oq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if oq.path != nil {
		_spec.Unique = true
	}
	if fields := oq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, oauth2application.FieldID)
		for i := range fields {
			if fields[i] != oauth2application.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := oq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := oq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := oq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := oq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (oq *Oauth2ApplicationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(oq.driver.Dialect())
	t1 := builder.Table(oauth2application.Table)
	columns := oq.ctx.Fields
	if len(columns) == 0 {
		columns = oauth2application.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if oq.sql != nil {
		selector = oq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if oq.ctx.Unique != nil && *oq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range oq.predicates {
		p(selector)
	}
	for _, p := range oq.order {
		p(selector)
	}
	if offset := oq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := oq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Oauth2ApplicationGroupBy is the group-by builder for Oauth2Application entities.
type Oauth2ApplicationGroupBy struct {
	selector
	build *Oauth2ApplicationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ogb *Oauth2ApplicationGroupBy) Aggregate(fns ...AggregateFunc) *Oauth2ApplicationGroupBy {
	ogb.fns = append(ogb.fns, fns...)
	return ogb
}

// Scan applies the selector query and scans the result into the given value.
func (ogb *Oauth2ApplicationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ogb.build.ctx, "GroupBy")
	if err := ogb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*Oauth2ApplicationQuery, *Oauth2ApplicationGroupBy](ctx, ogb.build, ogb, ogb.build.inters, v)
}

func (ogb *Oauth2ApplicationGroupBy) sqlScan(ctx context.Context, root *Oauth2ApplicationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ogb.fns))
	for _, fn := range ogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ogb.flds)+len(ogb.fns))
		for _, f := range *ogb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ogb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ogb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Oauth2ApplicationSelect is the builder for selecting fields of Oauth2Application entities.
type Oauth2ApplicationSelect struct {
	*Oauth2ApplicationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (os *Oauth2ApplicationSelect) Aggregate(fns ...AggregateFunc) *Oauth2ApplicationSelect {
	os.fns = append(os.fns, fns...)
	return os
}

// Scan applies the selector query and scans the result into the given value.
func (os *Oauth2ApplicationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, os.ctx, "Select")
	if err := os.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*Oauth2ApplicationQuery, *Oauth2ApplicationSelect](ctx, os.Oauth2ApplicationQuery, os, os.inters, v)
}

func (os *Oauth2ApplicationSelect) sqlScan(ctx context.Context, root *Oauth2ApplicationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(os.fns))
	for _, fn := range os.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*os.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := os.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
