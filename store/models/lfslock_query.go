// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/server/store/models/lfslock"
	"github.com/gitbundle/server/store/models/predicate"
)

// LfsLockQuery is the builder for querying LfsLock entities.
type LfsLockQuery struct {
	config
	ctx        *QueryContext
	order      []lfslock.OrderOption
	inters     []Interceptor
	predicates []predicate.LfsLock
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LfsLockQuery builder.
func (llq *LfsLockQuery) Where(ps ...predicate.LfsLock) *LfsLockQuery {
	llq.predicates = append(llq.predicates, ps...)
	return llq
}

// Limit the number of records to be returned by this query.
func (llq *LfsLockQuery) Limit(limit int) *LfsLockQuery {
	llq.ctx.Limit = &limit
	return llq
}

// Offset to start from.
func (llq *LfsLockQuery) Offset(offset int) *LfsLockQuery {
	llq.ctx.Offset = &offset
	return llq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (llq *LfsLockQuery) Unique(unique bool) *LfsLockQuery {
	llq.ctx.Unique = &unique
	return llq
}

// Order specifies how the records should be ordered.
func (llq *LfsLockQuery) Order(o ...lfslock.OrderOption) *LfsLockQuery {
	llq.order = append(llq.order, o...)
	return llq
}

// First returns the first LfsLock entity from the query.
// Returns a *NotFoundError when no LfsLock was found.
func (llq *LfsLockQuery) First(ctx context.Context) (*LfsLock, error) {
	nodes, err := llq.Limit(1).All(setContextOp(ctx, llq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{lfslock.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (llq *LfsLockQuery) FirstX(ctx context.Context) *LfsLock {
	node, err := llq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LfsLock ID from the query.
// Returns a *NotFoundError when no LfsLock ID was found.
func (llq *LfsLockQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = llq.Limit(1).IDs(setContextOp(ctx, llq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{lfslock.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (llq *LfsLockQuery) FirstIDX(ctx context.Context) int {
	id, err := llq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LfsLock entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LfsLock entity is found.
// Returns a *NotFoundError when no LfsLock entities are found.
func (llq *LfsLockQuery) Only(ctx context.Context) (*LfsLock, error) {
	nodes, err := llq.Limit(2).All(setContextOp(ctx, llq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{lfslock.Label}
	default:
		return nil, &NotSingularError{lfslock.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (llq *LfsLockQuery) OnlyX(ctx context.Context) *LfsLock {
	node, err := llq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LfsLock ID in the query.
// Returns a *NotSingularError when more than one LfsLock ID is found.
// Returns a *NotFoundError when no entities are found.
func (llq *LfsLockQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = llq.Limit(2).IDs(setContextOp(ctx, llq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{lfslock.Label}
	default:
		err = &NotSingularError{lfslock.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (llq *LfsLockQuery) OnlyIDX(ctx context.Context) int {
	id, err := llq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LfsLocks.
func (llq *LfsLockQuery) All(ctx context.Context) ([]*LfsLock, error) {
	ctx = setContextOp(ctx, llq.ctx, "All")
	if err := llq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LfsLock, *LfsLockQuery]()
	return withInterceptors[[]*LfsLock](ctx, llq, qr, llq.inters)
}

// AllX is like All, but panics if an error occurs.
func (llq *LfsLockQuery) AllX(ctx context.Context) []*LfsLock {
	nodes, err := llq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LfsLock IDs.
func (llq *LfsLockQuery) IDs(ctx context.Context) (ids []int, err error) {
	if llq.ctx.Unique == nil && llq.path != nil {
		llq.Unique(true)
	}
	ctx = setContextOp(ctx, llq.ctx, "IDs")
	if err = llq.Select(lfslock.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (llq *LfsLockQuery) IDsX(ctx context.Context) []int {
	ids, err := llq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (llq *LfsLockQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, llq.ctx, "Count")
	if err := llq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, llq, querierCount[*LfsLockQuery](), llq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (llq *LfsLockQuery) CountX(ctx context.Context) int {
	count, err := llq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (llq *LfsLockQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, llq.ctx, "Exist")
	switch _, err := llq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (llq *LfsLockQuery) ExistX(ctx context.Context) bool {
	exist, err := llq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LfsLockQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (llq *LfsLockQuery) Clone() *LfsLockQuery {
	if llq == nil {
		return nil
	}
	return &LfsLockQuery{
		config:     llq.config,
		ctx:        llq.ctx.Clone(),
		order:      append([]lfslock.OrderOption{}, llq.order...),
		inters:     append([]Interceptor{}, llq.inters...),
		predicates: append([]predicate.LfsLock{}, llq.predicates...),
		// clone intermediate query.
		sql:  llq.sql.Clone(),
		path: llq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RepoID int64 `json:"repo_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LfsLock.Query().
//		GroupBy(lfslock.FieldRepoID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (llq *LfsLockQuery) GroupBy(field string, fields ...string) *LfsLockGroupBy {
	llq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LfsLockGroupBy{build: llq}
	grbuild.flds = &llq.ctx.Fields
	grbuild.label = lfslock.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RepoID int64 `json:"repo_id,omitempty"`
//	}
//
//	client.LfsLock.Query().
//		Select(lfslock.FieldRepoID).
//		Scan(ctx, &v)
func (llq *LfsLockQuery) Select(fields ...string) *LfsLockSelect {
	llq.ctx.Fields = append(llq.ctx.Fields, fields...)
	sbuild := &LfsLockSelect{LfsLockQuery: llq}
	sbuild.label = lfslock.Label
	sbuild.flds, sbuild.scan = &llq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LfsLockSelect configured with the given aggregations.
func (llq *LfsLockQuery) Aggregate(fns ...AggregateFunc) *LfsLockSelect {
	return llq.Select().Aggregate(fns...)
}

func (llq *LfsLockQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range llq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, llq); err != nil {
				return err
			}
		}
	}
	for _, f := range llq.ctx.Fields {
		if !lfslock.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if llq.path != nil {
		prev, err := llq.path(ctx)
		if err != nil {
			return err
		}
		llq.sql = prev
	}
	return nil
}

func (llq *LfsLockQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LfsLock, error) {
	var (
		nodes = []*LfsLock{}
		_spec = llq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LfsLock).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LfsLock{config: llq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, llq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (llq *LfsLockQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := llq.querySpec()
	_spec.Node.Columns = llq.ctx.Fields
	if len(llq.ctx.Fields) > 0 {
		_spec.Unique = llq.ctx.Unique != nil && *llq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, llq.driver, _spec)
}

func (llq *LfsLockQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(lfslock.Table, lfslock.Columns, sqlgraph.NewFieldSpec(lfslock.FieldID, field.TypeInt))
	_spec.From = llq.sql
	if unique := llq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if llq.path != nil {
		_spec.Unique = true
	}
	if fields := llq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, lfslock.FieldID)
		for i := range fields {
			if fields[i] != lfslock.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := llq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := llq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := llq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := llq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (llq *LfsLockQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(llq.driver.Dialect())
	t1 := builder.Table(lfslock.Table)
	columns := llq.ctx.Fields
	if len(columns) == 0 {
		columns = lfslock.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if llq.sql != nil {
		selector = llq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if llq.ctx.Unique != nil && *llq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range llq.predicates {
		p(selector)
	}
	for _, p := range llq.order {
		p(selector)
	}
	if offset := llq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := llq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LfsLockGroupBy is the group-by builder for LfsLock entities.
type LfsLockGroupBy struct {
	selector
	build *LfsLockQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (llgb *LfsLockGroupBy) Aggregate(fns ...AggregateFunc) *LfsLockGroupBy {
	llgb.fns = append(llgb.fns, fns...)
	return llgb
}

// Scan applies the selector query and scans the result into the given value.
func (llgb *LfsLockGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, llgb.build.ctx, "GroupBy")
	if err := llgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LfsLockQuery, *LfsLockGroupBy](ctx, llgb.build, llgb, llgb.build.inters, v)
}

func (llgb *LfsLockGroupBy) sqlScan(ctx context.Context, root *LfsLockQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(llgb.fns))
	for _, fn := range llgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*llgb.flds)+len(llgb.fns))
		for _, f := range *llgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*llgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := llgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LfsLockSelect is the builder for selecting fields of LfsLock entities.
type LfsLockSelect struct {
	*LfsLockQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (lls *LfsLockSelect) Aggregate(fns ...AggregateFunc) *LfsLockSelect {
	lls.fns = append(lls.fns, fns...)
	return lls
}

// Scan applies the selector query and scans the result into the given value.
func (lls *LfsLockSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lls.ctx, "Select")
	if err := lls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LfsLockQuery, *LfsLockSelect](ctx, lls.LfsLockQuery, lls, lls.inters, v)
}

func (lls *LfsLockSelect) sqlScan(ctx context.Context, root *LfsLockQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(lls.fns))
	for _, fn := range lls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*lls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
