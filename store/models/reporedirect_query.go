// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/server/store/models/predicate"
	"github.com/gitbundle/server/store/models/reporedirect"
)

// RepoRedirectQuery is the builder for querying RepoRedirect entities.
type RepoRedirectQuery struct {
	config
	ctx        *QueryContext
	order      []reporedirect.OrderOption
	inters     []Interceptor
	predicates []predicate.RepoRedirect
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RepoRedirectQuery builder.
func (rrq *RepoRedirectQuery) Where(ps ...predicate.RepoRedirect) *RepoRedirectQuery {
	rrq.predicates = append(rrq.predicates, ps...)
	return rrq
}

// Limit the number of records to be returned by this query.
func (rrq *RepoRedirectQuery) Limit(limit int) *RepoRedirectQuery {
	rrq.ctx.Limit = &limit
	return rrq
}

// Offset to start from.
func (rrq *RepoRedirectQuery) Offset(offset int) *RepoRedirectQuery {
	rrq.ctx.Offset = &offset
	return rrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rrq *RepoRedirectQuery) Unique(unique bool) *RepoRedirectQuery {
	rrq.ctx.Unique = &unique
	return rrq
}

// Order specifies how the records should be ordered.
func (rrq *RepoRedirectQuery) Order(o ...reporedirect.OrderOption) *RepoRedirectQuery {
	rrq.order = append(rrq.order, o...)
	return rrq
}

// First returns the first RepoRedirect entity from the query.
// Returns a *NotFoundError when no RepoRedirect was found.
func (rrq *RepoRedirectQuery) First(ctx context.Context) (*RepoRedirect, error) {
	nodes, err := rrq.Limit(1).All(setContextOp(ctx, rrq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{reporedirect.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rrq *RepoRedirectQuery) FirstX(ctx context.Context) *RepoRedirect {
	node, err := rrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RepoRedirect ID from the query.
// Returns a *NotFoundError when no RepoRedirect ID was found.
func (rrq *RepoRedirectQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rrq.Limit(1).IDs(setContextOp(ctx, rrq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{reporedirect.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rrq *RepoRedirectQuery) FirstIDX(ctx context.Context) int {
	id, err := rrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RepoRedirect entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RepoRedirect entity is found.
// Returns a *NotFoundError when no RepoRedirect entities are found.
func (rrq *RepoRedirectQuery) Only(ctx context.Context) (*RepoRedirect, error) {
	nodes, err := rrq.Limit(2).All(setContextOp(ctx, rrq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{reporedirect.Label}
	default:
		return nil, &NotSingularError{reporedirect.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rrq *RepoRedirectQuery) OnlyX(ctx context.Context) *RepoRedirect {
	node, err := rrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RepoRedirect ID in the query.
// Returns a *NotSingularError when more than one RepoRedirect ID is found.
// Returns a *NotFoundError when no entities are found.
func (rrq *RepoRedirectQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rrq.Limit(2).IDs(setContextOp(ctx, rrq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{reporedirect.Label}
	default:
		err = &NotSingularError{reporedirect.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rrq *RepoRedirectQuery) OnlyIDX(ctx context.Context) int {
	id, err := rrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RepoRedirects.
func (rrq *RepoRedirectQuery) All(ctx context.Context) ([]*RepoRedirect, error) {
	ctx = setContextOp(ctx, rrq.ctx, "All")
	if err := rrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RepoRedirect, *RepoRedirectQuery]()
	return withInterceptors[[]*RepoRedirect](ctx, rrq, qr, rrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rrq *RepoRedirectQuery) AllX(ctx context.Context) []*RepoRedirect {
	nodes, err := rrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RepoRedirect IDs.
func (rrq *RepoRedirectQuery) IDs(ctx context.Context) (ids []int, err error) {
	if rrq.ctx.Unique == nil && rrq.path != nil {
		rrq.Unique(true)
	}
	ctx = setContextOp(ctx, rrq.ctx, "IDs")
	if err = rrq.Select(reporedirect.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rrq *RepoRedirectQuery) IDsX(ctx context.Context) []int {
	ids, err := rrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rrq *RepoRedirectQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rrq.ctx, "Count")
	if err := rrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rrq, querierCount[*RepoRedirectQuery](), rrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rrq *RepoRedirectQuery) CountX(ctx context.Context) int {
	count, err := rrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rrq *RepoRedirectQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rrq.ctx, "Exist")
	switch _, err := rrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rrq *RepoRedirectQuery) ExistX(ctx context.Context) bool {
	exist, err := rrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RepoRedirectQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rrq *RepoRedirectQuery) Clone() *RepoRedirectQuery {
	if rrq == nil {
		return nil
	}
	return &RepoRedirectQuery{
		config:     rrq.config,
		ctx:        rrq.ctx.Clone(),
		order:      append([]reporedirect.OrderOption{}, rrq.order...),
		inters:     append([]Interceptor{}, rrq.inters...),
		predicates: append([]predicate.RepoRedirect{}, rrq.predicates...),
		// clone intermediate query.
		sql:  rrq.sql.Clone(),
		path: rrq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		OwnerID int64 `json:"owner_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RepoRedirect.Query().
//		GroupBy(reporedirect.FieldOwnerID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (rrq *RepoRedirectQuery) GroupBy(field string, fields ...string) *RepoRedirectGroupBy {
	rrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RepoRedirectGroupBy{build: rrq}
	grbuild.flds = &rrq.ctx.Fields
	grbuild.label = reporedirect.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		OwnerID int64 `json:"owner_id,omitempty"`
//	}
//
//	client.RepoRedirect.Query().
//		Select(reporedirect.FieldOwnerID).
//		Scan(ctx, &v)
func (rrq *RepoRedirectQuery) Select(fields ...string) *RepoRedirectSelect {
	rrq.ctx.Fields = append(rrq.ctx.Fields, fields...)
	sbuild := &RepoRedirectSelect{RepoRedirectQuery: rrq}
	sbuild.label = reporedirect.Label
	sbuild.flds, sbuild.scan = &rrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RepoRedirectSelect configured with the given aggregations.
func (rrq *RepoRedirectQuery) Aggregate(fns ...AggregateFunc) *RepoRedirectSelect {
	return rrq.Select().Aggregate(fns...)
}

func (rrq *RepoRedirectQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rrq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rrq); err != nil {
				return err
			}
		}
	}
	for _, f := range rrq.ctx.Fields {
		if !reporedirect.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if rrq.path != nil {
		prev, err := rrq.path(ctx)
		if err != nil {
			return err
		}
		rrq.sql = prev
	}
	return nil
}

func (rrq *RepoRedirectQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RepoRedirect, error) {
	var (
		nodes = []*RepoRedirect{}
		_spec = rrq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RepoRedirect).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RepoRedirect{config: rrq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (rrq *RepoRedirectQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rrq.querySpec()
	_spec.Node.Columns = rrq.ctx.Fields
	if len(rrq.ctx.Fields) > 0 {
		_spec.Unique = rrq.ctx.Unique != nil && *rrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rrq.driver, _spec)
}

func (rrq *RepoRedirectQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(reporedirect.Table, reporedirect.Columns, sqlgraph.NewFieldSpec(reporedirect.FieldID, field.TypeInt))
	_spec.From = rrq.sql
	if unique := rrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rrq.path != nil {
		_spec.Unique = true
	}
	if fields := rrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, reporedirect.FieldID)
		for i := range fields {
			if fields[i] != reporedirect.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rrq *RepoRedirectQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rrq.driver.Dialect())
	t1 := builder.Table(reporedirect.Table)
	columns := rrq.ctx.Fields
	if len(columns) == 0 {
		columns = reporedirect.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rrq.sql != nil {
		selector = rrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rrq.ctx.Unique != nil && *rrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rrq.predicates {
		p(selector)
	}
	for _, p := range rrq.order {
		p(selector)
	}
	if offset := rrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RepoRedirectGroupBy is the group-by builder for RepoRedirect entities.
type RepoRedirectGroupBy struct {
	selector
	build *RepoRedirectQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rrgb *RepoRedirectGroupBy) Aggregate(fns ...AggregateFunc) *RepoRedirectGroupBy {
	rrgb.fns = append(rrgb.fns, fns...)
	return rrgb
}

// Scan applies the selector query and scans the result into the given value.
func (rrgb *RepoRedirectGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rrgb.build.ctx, "GroupBy")
	if err := rrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RepoRedirectQuery, *RepoRedirectGroupBy](ctx, rrgb.build, rrgb, rrgb.build.inters, v)
}

func (rrgb *RepoRedirectGroupBy) sqlScan(ctx context.Context, root *RepoRedirectQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rrgb.fns))
	for _, fn := range rrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rrgb.flds)+len(rrgb.fns))
		for _, f := range *rrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RepoRedirectSelect is the builder for selecting fields of RepoRedirect entities.
type RepoRedirectSelect struct {
	*RepoRedirectQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rrs *RepoRedirectSelect) Aggregate(fns ...AggregateFunc) *RepoRedirectSelect {
	rrs.fns = append(rrs.fns, fns...)
	return rrs
}

// Scan applies the selector query and scans the result into the given value.
func (rrs *RepoRedirectSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rrs.ctx, "Select")
	if err := rrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RepoRedirectQuery, *RepoRedirectSelect](ctx, rrs.RepoRedirectQuery, rrs, rrs.inters, v)
}

func (rrs *RepoRedirectSelect) sqlScan(ctx context.Context, root *RepoRedirectQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rrs.fns))
	for _, fn := range rrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
