// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/gitbundle/store/models/pkgversion"
	"github.com/gitbundle/gitbundle/store/models/predicate"
)

// PkgVersionQuery is the builder for querying PkgVersion entities.
type PkgVersionQuery struct {
	config
	ctx        *QueryContext
	order      []pkgversion.OrderOption
	inters     []Interceptor
	predicates []predicate.PkgVersion
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PkgVersionQuery builder.
func (pvq *PkgVersionQuery) Where(ps ...predicate.PkgVersion) *PkgVersionQuery {
	pvq.predicates = append(pvq.predicates, ps...)
	return pvq
}

// Limit the number of records to be returned by this query.
func (pvq *PkgVersionQuery) Limit(limit int) *PkgVersionQuery {
	pvq.ctx.Limit = &limit
	return pvq
}

// Offset to start from.
func (pvq *PkgVersionQuery) Offset(offset int) *PkgVersionQuery {
	pvq.ctx.Offset = &offset
	return pvq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pvq *PkgVersionQuery) Unique(unique bool) *PkgVersionQuery {
	pvq.ctx.Unique = &unique
	return pvq
}

// Order specifies how the records should be ordered.
func (pvq *PkgVersionQuery) Order(o ...pkgversion.OrderOption) *PkgVersionQuery {
	pvq.order = append(pvq.order, o...)
	return pvq
}

// First returns the first PkgVersion entity from the query.
// Returns a *NotFoundError when no PkgVersion was found.
func (pvq *PkgVersionQuery) First(ctx context.Context) (*PkgVersion, error) {
	nodes, err := pvq.Limit(1).All(setContextOp(ctx, pvq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{pkgversion.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pvq *PkgVersionQuery) FirstX(ctx context.Context) *PkgVersion {
	node, err := pvq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PkgVersion ID from the query.
// Returns a *NotFoundError when no PkgVersion ID was found.
func (pvq *PkgVersionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pvq.Limit(1).IDs(setContextOp(ctx, pvq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{pkgversion.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pvq *PkgVersionQuery) FirstIDX(ctx context.Context) int {
	id, err := pvq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PkgVersion entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PkgVersion entity is found.
// Returns a *NotFoundError when no PkgVersion entities are found.
func (pvq *PkgVersionQuery) Only(ctx context.Context) (*PkgVersion, error) {
	nodes, err := pvq.Limit(2).All(setContextOp(ctx, pvq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{pkgversion.Label}
	default:
		return nil, &NotSingularError{pkgversion.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pvq *PkgVersionQuery) OnlyX(ctx context.Context) *PkgVersion {
	node, err := pvq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PkgVersion ID in the query.
// Returns a *NotSingularError when more than one PkgVersion ID is found.
// Returns a *NotFoundError when no entities are found.
func (pvq *PkgVersionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pvq.Limit(2).IDs(setContextOp(ctx, pvq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{pkgversion.Label}
	default:
		err = &NotSingularError{pkgversion.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pvq *PkgVersionQuery) OnlyIDX(ctx context.Context) int {
	id, err := pvq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PkgVersions.
func (pvq *PkgVersionQuery) All(ctx context.Context) ([]*PkgVersion, error) {
	ctx = setContextOp(ctx, pvq.ctx, "All")
	if err := pvq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PkgVersion, *PkgVersionQuery]()
	return withInterceptors[[]*PkgVersion](ctx, pvq, qr, pvq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pvq *PkgVersionQuery) AllX(ctx context.Context) []*PkgVersion {
	nodes, err := pvq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PkgVersion IDs.
func (pvq *PkgVersionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if pvq.ctx.Unique == nil && pvq.path != nil {
		pvq.Unique(true)
	}
	ctx = setContextOp(ctx, pvq.ctx, "IDs")
	if err = pvq.Select(pkgversion.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pvq *PkgVersionQuery) IDsX(ctx context.Context) []int {
	ids, err := pvq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pvq *PkgVersionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pvq.ctx, "Count")
	if err := pvq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pvq, querierCount[*PkgVersionQuery](), pvq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pvq *PkgVersionQuery) CountX(ctx context.Context) int {
	count, err := pvq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pvq *PkgVersionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pvq.ctx, "Exist")
	switch _, err := pvq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pvq *PkgVersionQuery) ExistX(ctx context.Context) bool {
	exist, err := pvq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PkgVersionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pvq *PkgVersionQuery) Clone() *PkgVersionQuery {
	if pvq == nil {
		return nil
	}
	return &PkgVersionQuery{
		config:     pvq.config,
		ctx:        pvq.ctx.Clone(),
		order:      append([]pkgversion.OrderOption{}, pvq.order...),
		inters:     append([]Interceptor{}, pvq.inters...),
		predicates: append([]predicate.PkgVersion{}, pvq.predicates...),
		// clone intermediate query.
		sql:  pvq.sql.Clone(),
		path: pvq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PackageID int64 `json:"package_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PkgVersion.Query().
//		GroupBy(pkgversion.FieldPackageID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (pvq *PkgVersionQuery) GroupBy(field string, fields ...string) *PkgVersionGroupBy {
	pvq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PkgVersionGroupBy{build: pvq}
	grbuild.flds = &pvq.ctx.Fields
	grbuild.label = pkgversion.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PackageID int64 `json:"package_id,omitempty"`
//	}
//
//	client.PkgVersion.Query().
//		Select(pkgversion.FieldPackageID).
//		Scan(ctx, &v)
func (pvq *PkgVersionQuery) Select(fields ...string) *PkgVersionSelect {
	pvq.ctx.Fields = append(pvq.ctx.Fields, fields...)
	sbuild := &PkgVersionSelect{PkgVersionQuery: pvq}
	sbuild.label = pkgversion.Label
	sbuild.flds, sbuild.scan = &pvq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PkgVersionSelect configured with the given aggregations.
func (pvq *PkgVersionQuery) Aggregate(fns ...AggregateFunc) *PkgVersionSelect {
	return pvq.Select().Aggregate(fns...)
}

func (pvq *PkgVersionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pvq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pvq); err != nil {
				return err
			}
		}
	}
	for _, f := range pvq.ctx.Fields {
		if !pkgversion.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if pvq.path != nil {
		prev, err := pvq.path(ctx)
		if err != nil {
			return err
		}
		pvq.sql = prev
	}
	return nil
}

func (pvq *PkgVersionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PkgVersion, error) {
	var (
		nodes = []*PkgVersion{}
		_spec = pvq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PkgVersion).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PkgVersion{config: pvq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pvq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (pvq *PkgVersionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pvq.querySpec()
	_spec.Node.Columns = pvq.ctx.Fields
	if len(pvq.ctx.Fields) > 0 {
		_spec.Unique = pvq.ctx.Unique != nil && *pvq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pvq.driver, _spec)
}

func (pvq *PkgVersionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(pkgversion.Table, pkgversion.Columns, sqlgraph.NewFieldSpec(pkgversion.FieldID, field.TypeInt))
	_spec.From = pvq.sql
	if unique := pvq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pvq.path != nil {
		_spec.Unique = true
	}
	if fields := pvq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, pkgversion.FieldID)
		for i := range fields {
			if fields[i] != pkgversion.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pvq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pvq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pvq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pvq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pvq *PkgVersionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pvq.driver.Dialect())
	t1 := builder.Table(pkgversion.Table)
	columns := pvq.ctx.Fields
	if len(columns) == 0 {
		columns = pkgversion.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pvq.sql != nil {
		selector = pvq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pvq.ctx.Unique != nil && *pvq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pvq.predicates {
		p(selector)
	}
	for _, p := range pvq.order {
		p(selector)
	}
	if offset := pvq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pvq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PkgVersionGroupBy is the group-by builder for PkgVersion entities.
type PkgVersionGroupBy struct {
	selector
	build *PkgVersionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pvgb *PkgVersionGroupBy) Aggregate(fns ...AggregateFunc) *PkgVersionGroupBy {
	pvgb.fns = append(pvgb.fns, fns...)
	return pvgb
}

// Scan applies the selector query and scans the result into the given value.
func (pvgb *PkgVersionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pvgb.build.ctx, "GroupBy")
	if err := pvgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PkgVersionQuery, *PkgVersionGroupBy](ctx, pvgb.build, pvgb, pvgb.build.inters, v)
}

func (pvgb *PkgVersionGroupBy) sqlScan(ctx context.Context, root *PkgVersionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pvgb.fns))
	for _, fn := range pvgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pvgb.flds)+len(pvgb.fns))
		for _, f := range *pvgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pvgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pvgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PkgVersionSelect is the builder for selecting fields of PkgVersion entities.
type PkgVersionSelect struct {
	*PkgVersionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pvs *PkgVersionSelect) Aggregate(fns ...AggregateFunc) *PkgVersionSelect {
	pvs.fns = append(pvs.fns, fns...)
	return pvs
}

// Scan applies the selector query and scans the result into the given value.
func (pvs *PkgVersionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pvs.ctx, "Select")
	if err := pvs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PkgVersionQuery, *PkgVersionSelect](ctx, pvs.PkgVersionQuery, pvs, pvs.inters, v)
}

func (pvs *PkgVersionSelect) sqlScan(ctx context.Context, root *PkgVersionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pvs.fns))
	for _, fn := range pvs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pvs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pvs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
