// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/gitbundle/store/models/pkgblobupload"
	"github.com/gitbundle/gitbundle/store/models/predicate"
)

// PkgBlobUploadQuery is the builder for querying PkgBlobUpload entities.
type PkgBlobUploadQuery struct {
	config
	ctx        *QueryContext
	order      []pkgblobupload.OrderOption
	inters     []Interceptor
	predicates []predicate.PkgBlobUpload
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PkgBlobUploadQuery builder.
func (pbuq *PkgBlobUploadQuery) Where(ps ...predicate.PkgBlobUpload) *PkgBlobUploadQuery {
	pbuq.predicates = append(pbuq.predicates, ps...)
	return pbuq
}

// Limit the number of records to be returned by this query.
func (pbuq *PkgBlobUploadQuery) Limit(limit int) *PkgBlobUploadQuery {
	pbuq.ctx.Limit = &limit
	return pbuq
}

// Offset to start from.
func (pbuq *PkgBlobUploadQuery) Offset(offset int) *PkgBlobUploadQuery {
	pbuq.ctx.Offset = &offset
	return pbuq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pbuq *PkgBlobUploadQuery) Unique(unique bool) *PkgBlobUploadQuery {
	pbuq.ctx.Unique = &unique
	return pbuq
}

// Order specifies how the records should be ordered.
func (pbuq *PkgBlobUploadQuery) Order(o ...pkgblobupload.OrderOption) *PkgBlobUploadQuery {
	pbuq.order = append(pbuq.order, o...)
	return pbuq
}

// First returns the first PkgBlobUpload entity from the query.
// Returns a *NotFoundError when no PkgBlobUpload was found.
func (pbuq *PkgBlobUploadQuery) First(ctx context.Context) (*PkgBlobUpload, error) {
	nodes, err := pbuq.Limit(1).All(setContextOp(ctx, pbuq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{pkgblobupload.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pbuq *PkgBlobUploadQuery) FirstX(ctx context.Context) *PkgBlobUpload {
	node, err := pbuq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PkgBlobUpload ID from the query.
// Returns a *NotFoundError when no PkgBlobUpload ID was found.
func (pbuq *PkgBlobUploadQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pbuq.Limit(1).IDs(setContextOp(ctx, pbuq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{pkgblobupload.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pbuq *PkgBlobUploadQuery) FirstIDX(ctx context.Context) int {
	id, err := pbuq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PkgBlobUpload entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PkgBlobUpload entity is found.
// Returns a *NotFoundError when no PkgBlobUpload entities are found.
func (pbuq *PkgBlobUploadQuery) Only(ctx context.Context) (*PkgBlobUpload, error) {
	nodes, err := pbuq.Limit(2).All(setContextOp(ctx, pbuq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{pkgblobupload.Label}
	default:
		return nil, &NotSingularError{pkgblobupload.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pbuq *PkgBlobUploadQuery) OnlyX(ctx context.Context) *PkgBlobUpload {
	node, err := pbuq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PkgBlobUpload ID in the query.
// Returns a *NotSingularError when more than one PkgBlobUpload ID is found.
// Returns a *NotFoundError when no entities are found.
func (pbuq *PkgBlobUploadQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pbuq.Limit(2).IDs(setContextOp(ctx, pbuq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{pkgblobupload.Label}
	default:
		err = &NotSingularError{pkgblobupload.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pbuq *PkgBlobUploadQuery) OnlyIDX(ctx context.Context) int {
	id, err := pbuq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PkgBlobUploads.
func (pbuq *PkgBlobUploadQuery) All(ctx context.Context) ([]*PkgBlobUpload, error) {
	ctx = setContextOp(ctx, pbuq.ctx, "All")
	if err := pbuq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PkgBlobUpload, *PkgBlobUploadQuery]()
	return withInterceptors[[]*PkgBlobUpload](ctx, pbuq, qr, pbuq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pbuq *PkgBlobUploadQuery) AllX(ctx context.Context) []*PkgBlobUpload {
	nodes, err := pbuq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PkgBlobUpload IDs.
func (pbuq *PkgBlobUploadQuery) IDs(ctx context.Context) (ids []int, err error) {
	if pbuq.ctx.Unique == nil && pbuq.path != nil {
		pbuq.Unique(true)
	}
	ctx = setContextOp(ctx, pbuq.ctx, "IDs")
	if err = pbuq.Select(pkgblobupload.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pbuq *PkgBlobUploadQuery) IDsX(ctx context.Context) []int {
	ids, err := pbuq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pbuq *PkgBlobUploadQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pbuq.ctx, "Count")
	if err := pbuq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pbuq, querierCount[*PkgBlobUploadQuery](), pbuq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pbuq *PkgBlobUploadQuery) CountX(ctx context.Context) int {
	count, err := pbuq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pbuq *PkgBlobUploadQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pbuq.ctx, "Exist")
	switch _, err := pbuq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pbuq *PkgBlobUploadQuery) ExistX(ctx context.Context) bool {
	exist, err := pbuq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PkgBlobUploadQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pbuq *PkgBlobUploadQuery) Clone() *PkgBlobUploadQuery {
	if pbuq == nil {
		return nil
	}
	return &PkgBlobUploadQuery{
		config:     pbuq.config,
		ctx:        pbuq.ctx.Clone(),
		order:      append([]pkgblobupload.OrderOption{}, pbuq.order...),
		inters:     append([]Interceptor{}, pbuq.inters...),
		predicates: append([]predicate.PkgBlobUpload{}, pbuq.predicates...),
		// clone intermediate query.
		sql:  pbuq.sql.Clone(),
		path: pbuq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BlobUploadID string `json:"blob_upload_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PkgBlobUpload.Query().
//		GroupBy(pkgblobupload.FieldBlobUploadID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (pbuq *PkgBlobUploadQuery) GroupBy(field string, fields ...string) *PkgBlobUploadGroupBy {
	pbuq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PkgBlobUploadGroupBy{build: pbuq}
	grbuild.flds = &pbuq.ctx.Fields
	grbuild.label = pkgblobupload.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BlobUploadID string `json:"blob_upload_id,omitempty"`
//	}
//
//	client.PkgBlobUpload.Query().
//		Select(pkgblobupload.FieldBlobUploadID).
//		Scan(ctx, &v)
func (pbuq *PkgBlobUploadQuery) Select(fields ...string) *PkgBlobUploadSelect {
	pbuq.ctx.Fields = append(pbuq.ctx.Fields, fields...)
	sbuild := &PkgBlobUploadSelect{PkgBlobUploadQuery: pbuq}
	sbuild.label = pkgblobupload.Label
	sbuild.flds, sbuild.scan = &pbuq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PkgBlobUploadSelect configured with the given aggregations.
func (pbuq *PkgBlobUploadQuery) Aggregate(fns ...AggregateFunc) *PkgBlobUploadSelect {
	return pbuq.Select().Aggregate(fns...)
}

func (pbuq *PkgBlobUploadQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pbuq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pbuq); err != nil {
				return err
			}
		}
	}
	for _, f := range pbuq.ctx.Fields {
		if !pkgblobupload.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if pbuq.path != nil {
		prev, err := pbuq.path(ctx)
		if err != nil {
			return err
		}
		pbuq.sql = prev
	}
	return nil
}

func (pbuq *PkgBlobUploadQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PkgBlobUpload, error) {
	var (
		nodes = []*PkgBlobUpload{}
		_spec = pbuq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PkgBlobUpload).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PkgBlobUpload{config: pbuq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pbuq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (pbuq *PkgBlobUploadQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pbuq.querySpec()
	_spec.Node.Columns = pbuq.ctx.Fields
	if len(pbuq.ctx.Fields) > 0 {
		_spec.Unique = pbuq.ctx.Unique != nil && *pbuq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pbuq.driver, _spec)
}

func (pbuq *PkgBlobUploadQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(pkgblobupload.Table, pkgblobupload.Columns, sqlgraph.NewFieldSpec(pkgblobupload.FieldID, field.TypeInt))
	_spec.From = pbuq.sql
	if unique := pbuq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pbuq.path != nil {
		_spec.Unique = true
	}
	if fields := pbuq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, pkgblobupload.FieldID)
		for i := range fields {
			if fields[i] != pkgblobupload.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pbuq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pbuq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pbuq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pbuq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pbuq *PkgBlobUploadQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pbuq.driver.Dialect())
	t1 := builder.Table(pkgblobupload.Table)
	columns := pbuq.ctx.Fields
	if len(columns) == 0 {
		columns = pkgblobupload.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pbuq.sql != nil {
		selector = pbuq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pbuq.ctx.Unique != nil && *pbuq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pbuq.predicates {
		p(selector)
	}
	for _, p := range pbuq.order {
		p(selector)
	}
	if offset := pbuq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pbuq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PkgBlobUploadGroupBy is the group-by builder for PkgBlobUpload entities.
type PkgBlobUploadGroupBy struct {
	selector
	build *PkgBlobUploadQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pbugb *PkgBlobUploadGroupBy) Aggregate(fns ...AggregateFunc) *PkgBlobUploadGroupBy {
	pbugb.fns = append(pbugb.fns, fns...)
	return pbugb
}

// Scan applies the selector query and scans the result into the given value.
func (pbugb *PkgBlobUploadGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pbugb.build.ctx, "GroupBy")
	if err := pbugb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PkgBlobUploadQuery, *PkgBlobUploadGroupBy](ctx, pbugb.build, pbugb, pbugb.build.inters, v)
}

func (pbugb *PkgBlobUploadGroupBy) sqlScan(ctx context.Context, root *PkgBlobUploadQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pbugb.fns))
	for _, fn := range pbugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pbugb.flds)+len(pbugb.fns))
		for _, f := range *pbugb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pbugb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pbugb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PkgBlobUploadSelect is the builder for selecting fields of PkgBlobUpload entities.
type PkgBlobUploadSelect struct {
	*PkgBlobUploadQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pbus *PkgBlobUploadSelect) Aggregate(fns ...AggregateFunc) *PkgBlobUploadSelect {
	pbus.fns = append(pbus.fns, fns...)
	return pbus
}

// Scan applies the selector query and scans the result into the given value.
func (pbus *PkgBlobUploadSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pbus.ctx, "Select")
	if err := pbus.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PkgBlobUploadQuery, *PkgBlobUploadSelect](ctx, pbus.PkgBlobUploadQuery, pbus, pbus.inters, v)
}

func (pbus *PkgBlobUploadSelect) sqlScan(ctx context.Context, root *PkgBlobUploadQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pbus.fns))
	for _, fn := range pbus.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pbus.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pbus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
