// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/server/store/models/predicate"
	"github.com/gitbundle/server/store/models/repoarchiver"
)

// RepoArchiverQuery is the builder for querying RepoArchiver entities.
type RepoArchiverQuery struct {
	config
	ctx        *QueryContext
	order      []repoarchiver.OrderOption
	inters     []Interceptor
	predicates []predicate.RepoArchiver
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RepoArchiverQuery builder.
func (raq *RepoArchiverQuery) Where(ps ...predicate.RepoArchiver) *RepoArchiverQuery {
	raq.predicates = append(raq.predicates, ps...)
	return raq
}

// Limit the number of records to be returned by this query.
func (raq *RepoArchiverQuery) Limit(limit int) *RepoArchiverQuery {
	raq.ctx.Limit = &limit
	return raq
}

// Offset to start from.
func (raq *RepoArchiverQuery) Offset(offset int) *RepoArchiverQuery {
	raq.ctx.Offset = &offset
	return raq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (raq *RepoArchiverQuery) Unique(unique bool) *RepoArchiverQuery {
	raq.ctx.Unique = &unique
	return raq
}

// Order specifies how the records should be ordered.
func (raq *RepoArchiverQuery) Order(o ...repoarchiver.OrderOption) *RepoArchiverQuery {
	raq.order = append(raq.order, o...)
	return raq
}

// First returns the first RepoArchiver entity from the query.
// Returns a *NotFoundError when no RepoArchiver was found.
func (raq *RepoArchiverQuery) First(ctx context.Context) (*RepoArchiver, error) {
	nodes, err := raq.Limit(1).All(setContextOp(ctx, raq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{repoarchiver.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (raq *RepoArchiverQuery) FirstX(ctx context.Context) *RepoArchiver {
	node, err := raq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RepoArchiver ID from the query.
// Returns a *NotFoundError when no RepoArchiver ID was found.
func (raq *RepoArchiverQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = raq.Limit(1).IDs(setContextOp(ctx, raq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{repoarchiver.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (raq *RepoArchiverQuery) FirstIDX(ctx context.Context) int {
	id, err := raq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RepoArchiver entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RepoArchiver entity is found.
// Returns a *NotFoundError when no RepoArchiver entities are found.
func (raq *RepoArchiverQuery) Only(ctx context.Context) (*RepoArchiver, error) {
	nodes, err := raq.Limit(2).All(setContextOp(ctx, raq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{repoarchiver.Label}
	default:
		return nil, &NotSingularError{repoarchiver.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (raq *RepoArchiverQuery) OnlyX(ctx context.Context) *RepoArchiver {
	node, err := raq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RepoArchiver ID in the query.
// Returns a *NotSingularError when more than one RepoArchiver ID is found.
// Returns a *NotFoundError when no entities are found.
func (raq *RepoArchiverQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = raq.Limit(2).IDs(setContextOp(ctx, raq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{repoarchiver.Label}
	default:
		err = &NotSingularError{repoarchiver.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (raq *RepoArchiverQuery) OnlyIDX(ctx context.Context) int {
	id, err := raq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RepoArchivers.
func (raq *RepoArchiverQuery) All(ctx context.Context) ([]*RepoArchiver, error) {
	ctx = setContextOp(ctx, raq.ctx, "All")
	if err := raq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RepoArchiver, *RepoArchiverQuery]()
	return withInterceptors[[]*RepoArchiver](ctx, raq, qr, raq.inters)
}

// AllX is like All, but panics if an error occurs.
func (raq *RepoArchiverQuery) AllX(ctx context.Context) []*RepoArchiver {
	nodes, err := raq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RepoArchiver IDs.
func (raq *RepoArchiverQuery) IDs(ctx context.Context) (ids []int, err error) {
	if raq.ctx.Unique == nil && raq.path != nil {
		raq.Unique(true)
	}
	ctx = setContextOp(ctx, raq.ctx, "IDs")
	if err = raq.Select(repoarchiver.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (raq *RepoArchiverQuery) IDsX(ctx context.Context) []int {
	ids, err := raq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (raq *RepoArchiverQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, raq.ctx, "Count")
	if err := raq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, raq, querierCount[*RepoArchiverQuery](), raq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (raq *RepoArchiverQuery) CountX(ctx context.Context) int {
	count, err := raq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (raq *RepoArchiverQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, raq.ctx, "Exist")
	switch _, err := raq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (raq *RepoArchiverQuery) ExistX(ctx context.Context) bool {
	exist, err := raq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RepoArchiverQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (raq *RepoArchiverQuery) Clone() *RepoArchiverQuery {
	if raq == nil {
		return nil
	}
	return &RepoArchiverQuery{
		config:     raq.config,
		ctx:        raq.ctx.Clone(),
		order:      append([]repoarchiver.OrderOption{}, raq.order...),
		inters:     append([]Interceptor{}, raq.inters...),
		predicates: append([]predicate.RepoArchiver{}, raq.predicates...),
		// clone intermediate query.
		sql:  raq.sql.Clone(),
		path: raq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RepoID int64 `json:"repo_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RepoArchiver.Query().
//		GroupBy(repoarchiver.FieldRepoID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (raq *RepoArchiverQuery) GroupBy(field string, fields ...string) *RepoArchiverGroupBy {
	raq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RepoArchiverGroupBy{build: raq}
	grbuild.flds = &raq.ctx.Fields
	grbuild.label = repoarchiver.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RepoID int64 `json:"repo_id,omitempty"`
//	}
//
//	client.RepoArchiver.Query().
//		Select(repoarchiver.FieldRepoID).
//		Scan(ctx, &v)
func (raq *RepoArchiverQuery) Select(fields ...string) *RepoArchiverSelect {
	raq.ctx.Fields = append(raq.ctx.Fields, fields...)
	sbuild := &RepoArchiverSelect{RepoArchiverQuery: raq}
	sbuild.label = repoarchiver.Label
	sbuild.flds, sbuild.scan = &raq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RepoArchiverSelect configured with the given aggregations.
func (raq *RepoArchiverQuery) Aggregate(fns ...AggregateFunc) *RepoArchiverSelect {
	return raq.Select().Aggregate(fns...)
}

func (raq *RepoArchiverQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range raq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, raq); err != nil {
				return err
			}
		}
	}
	for _, f := range raq.ctx.Fields {
		if !repoarchiver.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if raq.path != nil {
		prev, err := raq.path(ctx)
		if err != nil {
			return err
		}
		raq.sql = prev
	}
	return nil
}

func (raq *RepoArchiverQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RepoArchiver, error) {
	var (
		nodes = []*RepoArchiver{}
		_spec = raq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RepoArchiver).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RepoArchiver{config: raq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, raq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (raq *RepoArchiverQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := raq.querySpec()
	_spec.Node.Columns = raq.ctx.Fields
	if len(raq.ctx.Fields) > 0 {
		_spec.Unique = raq.ctx.Unique != nil && *raq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, raq.driver, _spec)
}

func (raq *RepoArchiverQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(repoarchiver.Table, repoarchiver.Columns, sqlgraph.NewFieldSpec(repoarchiver.FieldID, field.TypeInt))
	_spec.From = raq.sql
	if unique := raq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if raq.path != nil {
		_spec.Unique = true
	}
	if fields := raq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, repoarchiver.FieldID)
		for i := range fields {
			if fields[i] != repoarchiver.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := raq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := raq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := raq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := raq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (raq *RepoArchiverQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(raq.driver.Dialect())
	t1 := builder.Table(repoarchiver.Table)
	columns := raq.ctx.Fields
	if len(columns) == 0 {
		columns = repoarchiver.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if raq.sql != nil {
		selector = raq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if raq.ctx.Unique != nil && *raq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range raq.predicates {
		p(selector)
	}
	for _, p := range raq.order {
		p(selector)
	}
	if offset := raq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := raq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RepoArchiverGroupBy is the group-by builder for RepoArchiver entities.
type RepoArchiverGroupBy struct {
	selector
	build *RepoArchiverQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ragb *RepoArchiverGroupBy) Aggregate(fns ...AggregateFunc) *RepoArchiverGroupBy {
	ragb.fns = append(ragb.fns, fns...)
	return ragb
}

// Scan applies the selector query and scans the result into the given value.
func (ragb *RepoArchiverGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ragb.build.ctx, "GroupBy")
	if err := ragb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RepoArchiverQuery, *RepoArchiverGroupBy](ctx, ragb.build, ragb, ragb.build.inters, v)
}

func (ragb *RepoArchiverGroupBy) sqlScan(ctx context.Context, root *RepoArchiverQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ragb.fns))
	for _, fn := range ragb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ragb.flds)+len(ragb.fns))
		for _, f := range *ragb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ragb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ragb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RepoArchiverSelect is the builder for selecting fields of RepoArchiver entities.
type RepoArchiverSelect struct {
	*RepoArchiverQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ras *RepoArchiverSelect) Aggregate(fns ...AggregateFunc) *RepoArchiverSelect {
	ras.fns = append(ras.fns, fns...)
	return ras
}

// Scan applies the selector query and scans the result into the given value.
func (ras *RepoArchiverSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ras.ctx, "Select")
	if err := ras.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RepoArchiverQuery, *RepoArchiverSelect](ctx, ras.RepoArchiverQuery, ras, ras.inters, v)
}

func (ras *RepoArchiverSelect) sqlScan(ctx context.Context, root *RepoArchiverQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ras.fns))
	for _, fn := range ras.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ras.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ras.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
