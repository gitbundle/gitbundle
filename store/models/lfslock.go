// Code generated by ent, DO NOT EDIT.

package models

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/gitbundle/server/store/models/lfslock"
)

// LfsLock is the model entity for the LfsLock schema.
type LfsLock struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// RepoID holds the value of the "repo_id" field.
	RepoID int64 `json:"repo_id,omitempty"`
	// OwnerID holds the value of the "owner_id" field.
	OwnerID int64 `json:"owner_id,omitempty"`
	// Path holds the value of the "path" field.
	Path string `json:"path,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*LfsLock) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case lfslock.FieldID, lfslock.FieldRepoID, lfslock.FieldOwnerID:
			values[i] = new(sql.NullInt64)
		case lfslock.FieldPath:
			values[i] = new(sql.NullString)
		case lfslock.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the LfsLock fields.
func (ll *LfsLock) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case lfslock.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ll.ID = int(value.Int64)
		case lfslock.FieldRepoID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field repo_id", values[i])
			} else if value.Valid {
				ll.RepoID = value.Int64
			}
		case lfslock.FieldOwnerID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field owner_id", values[i])
			} else if value.Valid {
				ll.OwnerID = value.Int64
			}
		case lfslock.FieldPath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field path", values[i])
			} else if value.Valid {
				ll.Path = value.String
			}
		case lfslock.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ll.CreatedAt = value.Time
			}
		default:
			ll.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the LfsLock.
// This includes values selected through modifiers, order, etc.
func (ll *LfsLock) Value(name string) (ent.Value, error) {
	return ll.selectValues.Get(name)
}

// Update returns a builder for updating this LfsLock.
// Note that you need to call LfsLock.Unwrap() before calling this method if this LfsLock
// was returned from a transaction, and the transaction was committed or rolled back.
func (ll *LfsLock) Update() *LfsLockUpdateOne {
	return NewLfsLockClient(ll.config).UpdateOne(ll)
}

// Unwrap unwraps the LfsLock entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ll *LfsLock) Unwrap() *LfsLock {
	_tx, ok := ll.config.driver.(*txDriver)
	if !ok {
		panic("models: LfsLock is not a transactional entity")
	}
	ll.config.driver = _tx.drv
	return ll
}

// String implements the fmt.Stringer.
func (ll *LfsLock) String() string {
	var builder strings.Builder
	builder.WriteString("LfsLock(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ll.ID))
	builder.WriteString("repo_id=")
	builder.WriteString(fmt.Sprintf("%v", ll.RepoID))
	builder.WriteString(", ")
	builder.WriteString("owner_id=")
	builder.WriteString(fmt.Sprintf("%v", ll.OwnerID))
	builder.WriteString(", ")
	builder.WriteString("path=")
	builder.WriteString(ll.Path)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ll.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// LfsLocks is a parsable slice of LfsLock.
type LfsLocks []*LfsLock
