// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/server/store/models/predicate"
	"github.com/gitbundle/server/store/models/renamedbranch"
)

// RenamedBranchQuery is the builder for querying RenamedBranch entities.
type RenamedBranchQuery struct {
	config
	ctx        *QueryContext
	order      []renamedbranch.OrderOption
	inters     []Interceptor
	predicates []predicate.RenamedBranch
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RenamedBranchQuery builder.
func (rbq *RenamedBranchQuery) Where(ps ...predicate.RenamedBranch) *RenamedBranchQuery {
	rbq.predicates = append(rbq.predicates, ps...)
	return rbq
}

// Limit the number of records to be returned by this query.
func (rbq *RenamedBranchQuery) Limit(limit int) *RenamedBranchQuery {
	rbq.ctx.Limit = &limit
	return rbq
}

// Offset to start from.
func (rbq *RenamedBranchQuery) Offset(offset int) *RenamedBranchQuery {
	rbq.ctx.Offset = &offset
	return rbq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rbq *RenamedBranchQuery) Unique(unique bool) *RenamedBranchQuery {
	rbq.ctx.Unique = &unique
	return rbq
}

// Order specifies how the records should be ordered.
func (rbq *RenamedBranchQuery) Order(o ...renamedbranch.OrderOption) *RenamedBranchQuery {
	rbq.order = append(rbq.order, o...)
	return rbq
}

// First returns the first RenamedBranch entity from the query.
// Returns a *NotFoundError when no RenamedBranch was found.
func (rbq *RenamedBranchQuery) First(ctx context.Context) (*RenamedBranch, error) {
	nodes, err := rbq.Limit(1).All(setContextOp(ctx, rbq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{renamedbranch.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rbq *RenamedBranchQuery) FirstX(ctx context.Context) *RenamedBranch {
	node, err := rbq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RenamedBranch ID from the query.
// Returns a *NotFoundError when no RenamedBranch ID was found.
func (rbq *RenamedBranchQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rbq.Limit(1).IDs(setContextOp(ctx, rbq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{renamedbranch.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rbq *RenamedBranchQuery) FirstIDX(ctx context.Context) int {
	id, err := rbq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RenamedBranch entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RenamedBranch entity is found.
// Returns a *NotFoundError when no RenamedBranch entities are found.
func (rbq *RenamedBranchQuery) Only(ctx context.Context) (*RenamedBranch, error) {
	nodes, err := rbq.Limit(2).All(setContextOp(ctx, rbq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{renamedbranch.Label}
	default:
		return nil, &NotSingularError{renamedbranch.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rbq *RenamedBranchQuery) OnlyX(ctx context.Context) *RenamedBranch {
	node, err := rbq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RenamedBranch ID in the query.
// Returns a *NotSingularError when more than one RenamedBranch ID is found.
// Returns a *NotFoundError when no entities are found.
func (rbq *RenamedBranchQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rbq.Limit(2).IDs(setContextOp(ctx, rbq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{renamedbranch.Label}
	default:
		err = &NotSingularError{renamedbranch.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rbq *RenamedBranchQuery) OnlyIDX(ctx context.Context) int {
	id, err := rbq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RenamedBranches.
func (rbq *RenamedBranchQuery) All(ctx context.Context) ([]*RenamedBranch, error) {
	ctx = setContextOp(ctx, rbq.ctx, "All")
	if err := rbq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RenamedBranch, *RenamedBranchQuery]()
	return withInterceptors[[]*RenamedBranch](ctx, rbq, qr, rbq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rbq *RenamedBranchQuery) AllX(ctx context.Context) []*RenamedBranch {
	nodes, err := rbq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RenamedBranch IDs.
func (rbq *RenamedBranchQuery) IDs(ctx context.Context) (ids []int, err error) {
	if rbq.ctx.Unique == nil && rbq.path != nil {
		rbq.Unique(true)
	}
	ctx = setContextOp(ctx, rbq.ctx, "IDs")
	if err = rbq.Select(renamedbranch.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rbq *RenamedBranchQuery) IDsX(ctx context.Context) []int {
	ids, err := rbq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rbq *RenamedBranchQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rbq.ctx, "Count")
	if err := rbq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rbq, querierCount[*RenamedBranchQuery](), rbq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rbq *RenamedBranchQuery) CountX(ctx context.Context) int {
	count, err := rbq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rbq *RenamedBranchQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rbq.ctx, "Exist")
	switch _, err := rbq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rbq *RenamedBranchQuery) ExistX(ctx context.Context) bool {
	exist, err := rbq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RenamedBranchQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rbq *RenamedBranchQuery) Clone() *RenamedBranchQuery {
	if rbq == nil {
		return nil
	}
	return &RenamedBranchQuery{
		config:     rbq.config,
		ctx:        rbq.ctx.Clone(),
		order:      append([]renamedbranch.OrderOption{}, rbq.order...),
		inters:     append([]Interceptor{}, rbq.inters...),
		predicates: append([]predicate.RenamedBranch{}, rbq.predicates...),
		// clone intermediate query.
		sql:  rbq.sql.Clone(),
		path: rbq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RepoID int64 `json:"repo_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RenamedBranch.Query().
//		GroupBy(renamedbranch.FieldRepoID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (rbq *RenamedBranchQuery) GroupBy(field string, fields ...string) *RenamedBranchGroupBy {
	rbq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RenamedBranchGroupBy{build: rbq}
	grbuild.flds = &rbq.ctx.Fields
	grbuild.label = renamedbranch.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RepoID int64 `json:"repo_id,omitempty"`
//	}
//
//	client.RenamedBranch.Query().
//		Select(renamedbranch.FieldRepoID).
//		Scan(ctx, &v)
func (rbq *RenamedBranchQuery) Select(fields ...string) *RenamedBranchSelect {
	rbq.ctx.Fields = append(rbq.ctx.Fields, fields...)
	sbuild := &RenamedBranchSelect{RenamedBranchQuery: rbq}
	sbuild.label = renamedbranch.Label
	sbuild.flds, sbuild.scan = &rbq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RenamedBranchSelect configured with the given aggregations.
func (rbq *RenamedBranchQuery) Aggregate(fns ...AggregateFunc) *RenamedBranchSelect {
	return rbq.Select().Aggregate(fns...)
}

func (rbq *RenamedBranchQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rbq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rbq); err != nil {
				return err
			}
		}
	}
	for _, f := range rbq.ctx.Fields {
		if !renamedbranch.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if rbq.path != nil {
		prev, err := rbq.path(ctx)
		if err != nil {
			return err
		}
		rbq.sql = prev
	}
	return nil
}

func (rbq *RenamedBranchQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RenamedBranch, error) {
	var (
		nodes = []*RenamedBranch{}
		_spec = rbq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RenamedBranch).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RenamedBranch{config: rbq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rbq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (rbq *RenamedBranchQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rbq.querySpec()
	_spec.Node.Columns = rbq.ctx.Fields
	if len(rbq.ctx.Fields) > 0 {
		_spec.Unique = rbq.ctx.Unique != nil && *rbq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rbq.driver, _spec)
}

func (rbq *RenamedBranchQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(renamedbranch.Table, renamedbranch.Columns, sqlgraph.NewFieldSpec(renamedbranch.FieldID, field.TypeInt))
	_spec.From = rbq.sql
	if unique := rbq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rbq.path != nil {
		_spec.Unique = true
	}
	if fields := rbq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, renamedbranch.FieldID)
		for i := range fields {
			if fields[i] != renamedbranch.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rbq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rbq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rbq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rbq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rbq *RenamedBranchQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rbq.driver.Dialect())
	t1 := builder.Table(renamedbranch.Table)
	columns := rbq.ctx.Fields
	if len(columns) == 0 {
		columns = renamedbranch.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rbq.sql != nil {
		selector = rbq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rbq.ctx.Unique != nil && *rbq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rbq.predicates {
		p(selector)
	}
	for _, p := range rbq.order {
		p(selector)
	}
	if offset := rbq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rbq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RenamedBranchGroupBy is the group-by builder for RenamedBranch entities.
type RenamedBranchGroupBy struct {
	selector
	build *RenamedBranchQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rbgb *RenamedBranchGroupBy) Aggregate(fns ...AggregateFunc) *RenamedBranchGroupBy {
	rbgb.fns = append(rbgb.fns, fns...)
	return rbgb
}

// Scan applies the selector query and scans the result into the given value.
func (rbgb *RenamedBranchGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rbgb.build.ctx, "GroupBy")
	if err := rbgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RenamedBranchQuery, *RenamedBranchGroupBy](ctx, rbgb.build, rbgb, rbgb.build.inters, v)
}

func (rbgb *RenamedBranchGroupBy) sqlScan(ctx context.Context, root *RenamedBranchQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rbgb.fns))
	for _, fn := range rbgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rbgb.flds)+len(rbgb.fns))
		for _, f := range *rbgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rbgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rbgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RenamedBranchSelect is the builder for selecting fields of RenamedBranch entities.
type RenamedBranchSelect struct {
	*RenamedBranchQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rbs *RenamedBranchSelect) Aggregate(fns ...AggregateFunc) *RenamedBranchSelect {
	rbs.fns = append(rbs.fns, fns...)
	return rbs
}

// Scan applies the selector query and scans the result into the given value.
func (rbs *RenamedBranchSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rbs.ctx, "Select")
	if err := rbs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RenamedBranchQuery, *RenamedBranchSelect](ctx, rbs.RenamedBranchQuery, rbs, rbs.inters, v)
}

func (rbs *RenamedBranchSelect) sqlScan(ctx context.Context, root *RenamedBranchQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rbs.fns))
	for _, fn := range rbs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rbs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rbs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
