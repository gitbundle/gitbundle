// Code generated by ent, DO NOT EDIT.

package models

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/gitbundle/gitbundle/store/models/mirror"
)

// Mirror is the model entity for the Mirror schema.
type Mirror struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// RepoID holds the value of the "repo_id" field.
	RepoID int64 `json:"repo_id,omitempty"`
	// Interval holds the value of the "interval" field.
	Interval int64 `json:"interval,omitempty"`
	// EnablePrune holds the value of the "enable_prune" field.
	EnablePrune bool `json:"enable_prune,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// NextUpdatedAt holds the value of the "next_updated_at" field.
	NextUpdatedAt time.Time `json:"next_updated_at,omitempty"`
	// LfsEnabled holds the value of the "lfs_enabled" field.
	LfsEnabled bool `json:"lfs_enabled,omitempty"`
	// LfsEndpoint holds the value of the "lfs_endpoint" field.
	LfsEndpoint  string `json:"lfs_endpoint,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Mirror) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case mirror.FieldEnablePrune, mirror.FieldLfsEnabled:
			values[i] = new(sql.NullBool)
		case mirror.FieldID, mirror.FieldRepoID, mirror.FieldInterval:
			values[i] = new(sql.NullInt64)
		case mirror.FieldLfsEndpoint:
			values[i] = new(sql.NullString)
		case mirror.FieldUpdatedAt, mirror.FieldNextUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Mirror fields.
func (m *Mirror) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case mirror.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			m.ID = int(value.Int64)
		case mirror.FieldRepoID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field repo_id", values[i])
			} else if value.Valid {
				m.RepoID = value.Int64
			}
		case mirror.FieldInterval:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field interval", values[i])
			} else if value.Valid {
				m.Interval = value.Int64
			}
		case mirror.FieldEnablePrune:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field enable_prune", values[i])
			} else if value.Valid {
				m.EnablePrune = value.Bool
			}
		case mirror.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				m.UpdatedAt = value.Time
			}
		case mirror.FieldNextUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field next_updated_at", values[i])
			} else if value.Valid {
				m.NextUpdatedAt = value.Time
			}
		case mirror.FieldLfsEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field lfs_enabled", values[i])
			} else if value.Valid {
				m.LfsEnabled = value.Bool
			}
		case mirror.FieldLfsEndpoint:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field lfs_endpoint", values[i])
			} else if value.Valid {
				m.LfsEndpoint = value.String
			}
		default:
			m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Mirror.
// This includes values selected through modifiers, order, etc.
func (m *Mirror) Value(name string) (ent.Value, error) {
	return m.selectValues.Get(name)
}

// Update returns a builder for updating this Mirror.
// Note that you need to call Mirror.Unwrap() before calling this method if this Mirror
// was returned from a transaction, and the transaction was committed or rolled back.
func (m *Mirror) Update() *MirrorUpdateOne {
	return NewMirrorClient(m.config).UpdateOne(m)
}

// Unwrap unwraps the Mirror entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (m *Mirror) Unwrap() *Mirror {
	_tx, ok := m.config.driver.(*txDriver)
	if !ok {
		panic("models: Mirror is not a transactional entity")
	}
	m.config.driver = _tx.drv
	return m
}

// String implements the fmt.Stringer.
func (m *Mirror) String() string {
	var builder strings.Builder
	builder.WriteString("Mirror(")
	builder.WriteString(fmt.Sprintf("id=%v, ", m.ID))
	builder.WriteString("repo_id=")
	builder.WriteString(fmt.Sprintf("%v", m.RepoID))
	builder.WriteString(", ")
	builder.WriteString("interval=")
	builder.WriteString(fmt.Sprintf("%v", m.Interval))
	builder.WriteString(", ")
	builder.WriteString("enable_prune=")
	builder.WriteString(fmt.Sprintf("%v", m.EnablePrune))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("next_updated_at=")
	builder.WriteString(m.NextUpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("lfs_enabled=")
	builder.WriteString(fmt.Sprintf("%v", m.LfsEnabled))
	builder.WriteString(", ")
	builder.WriteString("lfs_endpoint=")
	builder.WriteString(m.LfsEndpoint)
	builder.WriteByte(')')
	return builder.String()
}

// Mirrors is a parsable slice of Mirror.
type Mirrors []*Mirror
