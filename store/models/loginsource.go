// Code generated by ent, DO NOT EDIT.

package models

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/gitbundle/gitbundle/store/models/loginsource"
)

// LoginSource is the model entity for the LoginSource schema.
type LoginSource struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Type holds the value of the "type" field.
	Type int `json:"type,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// IsActive holds the value of the "is_active" field.
	IsActive bool `json:"is_active,omitempty"`
	// IsSyncEnabled holds the value of the "is_sync_enabled" field.
	IsSyncEnabled bool `json:"is_sync_enabled,omitempty"`
	// Config holds the value of the "config" field.
	Config string `json:"config,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*LoginSource) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case loginsource.FieldIsActive, loginsource.FieldIsSyncEnabled:
			values[i] = new(sql.NullBool)
		case loginsource.FieldID, loginsource.FieldType:
			values[i] = new(sql.NullInt64)
		case loginsource.FieldName, loginsource.FieldConfig:
			values[i] = new(sql.NullString)
		case loginsource.FieldCreatedAt, loginsource.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the LoginSource fields.
func (ls *LoginSource) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case loginsource.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ls.ID = int(value.Int64)
		case loginsource.FieldType:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				ls.Type = int(value.Int64)
			}
		case loginsource.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				ls.Name = value.String
			}
		case loginsource.FieldIsActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_active", values[i])
			} else if value.Valid {
				ls.IsActive = value.Bool
			}
		case loginsource.FieldIsSyncEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_sync_enabled", values[i])
			} else if value.Valid {
				ls.IsSyncEnabled = value.Bool
			}
		case loginsource.FieldConfig:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field config", values[i])
			} else if value.Valid {
				ls.Config = value.String
			}
		case loginsource.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ls.CreatedAt = value.Time
			}
		case loginsource.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				ls.UpdatedAt = value.Time
			}
		default:
			ls.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the LoginSource.
// This includes values selected through modifiers, order, etc.
func (ls *LoginSource) Value(name string) (ent.Value, error) {
	return ls.selectValues.Get(name)
}

// Update returns a builder for updating this LoginSource.
// Note that you need to call LoginSource.Unwrap() before calling this method if this LoginSource
// was returned from a transaction, and the transaction was committed or rolled back.
func (ls *LoginSource) Update() *LoginSourceUpdateOne {
	return NewLoginSourceClient(ls.config).UpdateOne(ls)
}

// Unwrap unwraps the LoginSource entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ls *LoginSource) Unwrap() *LoginSource {
	_tx, ok := ls.config.driver.(*txDriver)
	if !ok {
		panic("models: LoginSource is not a transactional entity")
	}
	ls.config.driver = _tx.drv
	return ls
}

// String implements the fmt.Stringer.
func (ls *LoginSource) String() string {
	var builder strings.Builder
	builder.WriteString("LoginSource(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ls.ID))
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", ls.Type))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(ls.Name)
	builder.WriteString(", ")
	builder.WriteString("is_active=")
	builder.WriteString(fmt.Sprintf("%v", ls.IsActive))
	builder.WriteString(", ")
	builder.WriteString("is_sync_enabled=")
	builder.WriteString(fmt.Sprintf("%v", ls.IsSyncEnabled))
	builder.WriteString(", ")
	builder.WriteString("config=")
	builder.WriteString(ls.Config)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ls.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(ls.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// LoginSources is a parsable slice of LoginSource.
type LoginSources []*LoginSource
