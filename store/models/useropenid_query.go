// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/gitbundle/store/models/predicate"
	"github.com/gitbundle/gitbundle/store/models/useropenid"
)

// UserOpenidQuery is the builder for querying UserOpenid entities.
type UserOpenidQuery struct {
	config
	ctx        *QueryContext
	order      []useropenid.OrderOption
	inters     []Interceptor
	predicates []predicate.UserOpenid
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserOpenidQuery builder.
func (uoq *UserOpenidQuery) Where(ps ...predicate.UserOpenid) *UserOpenidQuery {
	uoq.predicates = append(uoq.predicates, ps...)
	return uoq
}

// Limit the number of records to be returned by this query.
func (uoq *UserOpenidQuery) Limit(limit int) *UserOpenidQuery {
	uoq.ctx.Limit = &limit
	return uoq
}

// Offset to start from.
func (uoq *UserOpenidQuery) Offset(offset int) *UserOpenidQuery {
	uoq.ctx.Offset = &offset
	return uoq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (uoq *UserOpenidQuery) Unique(unique bool) *UserOpenidQuery {
	uoq.ctx.Unique = &unique
	return uoq
}

// Order specifies how the records should be ordered.
func (uoq *UserOpenidQuery) Order(o ...useropenid.OrderOption) *UserOpenidQuery {
	uoq.order = append(uoq.order, o...)
	return uoq
}

// First returns the first UserOpenid entity from the query.
// Returns a *NotFoundError when no UserOpenid was found.
func (uoq *UserOpenidQuery) First(ctx context.Context) (*UserOpenid, error) {
	nodes, err := uoq.Limit(1).All(setContextOp(ctx, uoq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{useropenid.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (uoq *UserOpenidQuery) FirstX(ctx context.Context) *UserOpenid {
	node, err := uoq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserOpenid ID from the query.
// Returns a *NotFoundError when no UserOpenid ID was found.
func (uoq *UserOpenidQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uoq.Limit(1).IDs(setContextOp(ctx, uoq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{useropenid.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (uoq *UserOpenidQuery) FirstIDX(ctx context.Context) int {
	id, err := uoq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserOpenid entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserOpenid entity is found.
// Returns a *NotFoundError when no UserOpenid entities are found.
func (uoq *UserOpenidQuery) Only(ctx context.Context) (*UserOpenid, error) {
	nodes, err := uoq.Limit(2).All(setContextOp(ctx, uoq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{useropenid.Label}
	default:
		return nil, &NotSingularError{useropenid.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (uoq *UserOpenidQuery) OnlyX(ctx context.Context) *UserOpenid {
	node, err := uoq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserOpenid ID in the query.
// Returns a *NotSingularError when more than one UserOpenid ID is found.
// Returns a *NotFoundError when no entities are found.
func (uoq *UserOpenidQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uoq.Limit(2).IDs(setContextOp(ctx, uoq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{useropenid.Label}
	default:
		err = &NotSingularError{useropenid.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (uoq *UserOpenidQuery) OnlyIDX(ctx context.Context) int {
	id, err := uoq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserOpenids.
func (uoq *UserOpenidQuery) All(ctx context.Context) ([]*UserOpenid, error) {
	ctx = setContextOp(ctx, uoq.ctx, "All")
	if err := uoq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserOpenid, *UserOpenidQuery]()
	return withInterceptors[[]*UserOpenid](ctx, uoq, qr, uoq.inters)
}

// AllX is like All, but panics if an error occurs.
func (uoq *UserOpenidQuery) AllX(ctx context.Context) []*UserOpenid {
	nodes, err := uoq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserOpenid IDs.
func (uoq *UserOpenidQuery) IDs(ctx context.Context) (ids []int, err error) {
	if uoq.ctx.Unique == nil && uoq.path != nil {
		uoq.Unique(true)
	}
	ctx = setContextOp(ctx, uoq.ctx, "IDs")
	if err = uoq.Select(useropenid.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (uoq *UserOpenidQuery) IDsX(ctx context.Context) []int {
	ids, err := uoq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (uoq *UserOpenidQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, uoq.ctx, "Count")
	if err := uoq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, uoq, querierCount[*UserOpenidQuery](), uoq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (uoq *UserOpenidQuery) CountX(ctx context.Context) int {
	count, err := uoq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (uoq *UserOpenidQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, uoq.ctx, "Exist")
	switch _, err := uoq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (uoq *UserOpenidQuery) ExistX(ctx context.Context) bool {
	exist, err := uoq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserOpenidQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (uoq *UserOpenidQuery) Clone() *UserOpenidQuery {
	if uoq == nil {
		return nil
	}
	return &UserOpenidQuery{
		config:     uoq.config,
		ctx:        uoq.ctx.Clone(),
		order:      append([]useropenid.OrderOption{}, uoq.order...),
		inters:     append([]Interceptor{}, uoq.inters...),
		predicates: append([]predicate.UserOpenid{}, uoq.predicates...),
		// clone intermediate query.
		sql:  uoq.sql.Clone(),
		path: uoq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int64 `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserOpenid.Query().
//		GroupBy(useropenid.FieldUserID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (uoq *UserOpenidQuery) GroupBy(field string, fields ...string) *UserOpenidGroupBy {
	uoq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserOpenidGroupBy{build: uoq}
	grbuild.flds = &uoq.ctx.Fields
	grbuild.label = useropenid.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int64 `json:"user_id,omitempty"`
//	}
//
//	client.UserOpenid.Query().
//		Select(useropenid.FieldUserID).
//		Scan(ctx, &v)
func (uoq *UserOpenidQuery) Select(fields ...string) *UserOpenidSelect {
	uoq.ctx.Fields = append(uoq.ctx.Fields, fields...)
	sbuild := &UserOpenidSelect{UserOpenidQuery: uoq}
	sbuild.label = useropenid.Label
	sbuild.flds, sbuild.scan = &uoq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserOpenidSelect configured with the given aggregations.
func (uoq *UserOpenidQuery) Aggregate(fns ...AggregateFunc) *UserOpenidSelect {
	return uoq.Select().Aggregate(fns...)
}

func (uoq *UserOpenidQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range uoq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, uoq); err != nil {
				return err
			}
		}
	}
	for _, f := range uoq.ctx.Fields {
		if !useropenid.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if uoq.path != nil {
		prev, err := uoq.path(ctx)
		if err != nil {
			return err
		}
		uoq.sql = prev
	}
	return nil
}

func (uoq *UserOpenidQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserOpenid, error) {
	var (
		nodes = []*UserOpenid{}
		_spec = uoq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserOpenid).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserOpenid{config: uoq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, uoq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (uoq *UserOpenidQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := uoq.querySpec()
	_spec.Node.Columns = uoq.ctx.Fields
	if len(uoq.ctx.Fields) > 0 {
		_spec.Unique = uoq.ctx.Unique != nil && *uoq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, uoq.driver, _spec)
}

func (uoq *UserOpenidQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(useropenid.Table, useropenid.Columns, sqlgraph.NewFieldSpec(useropenid.FieldID, field.TypeInt))
	_spec.From = uoq.sql
	if unique := uoq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if uoq.path != nil {
		_spec.Unique = true
	}
	if fields := uoq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, useropenid.FieldID)
		for i := range fields {
			if fields[i] != useropenid.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := uoq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := uoq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := uoq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := uoq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (uoq *UserOpenidQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(uoq.driver.Dialect())
	t1 := builder.Table(useropenid.Table)
	columns := uoq.ctx.Fields
	if len(columns) == 0 {
		columns = useropenid.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if uoq.sql != nil {
		selector = uoq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if uoq.ctx.Unique != nil && *uoq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range uoq.predicates {
		p(selector)
	}
	for _, p := range uoq.order {
		p(selector)
	}
	if offset := uoq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := uoq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserOpenidGroupBy is the group-by builder for UserOpenid entities.
type UserOpenidGroupBy struct {
	selector
	build *UserOpenidQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uogb *UserOpenidGroupBy) Aggregate(fns ...AggregateFunc) *UserOpenidGroupBy {
	uogb.fns = append(uogb.fns, fns...)
	return uogb
}

// Scan applies the selector query and scans the result into the given value.
func (uogb *UserOpenidGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uogb.build.ctx, "GroupBy")
	if err := uogb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserOpenidQuery, *UserOpenidGroupBy](ctx, uogb.build, uogb, uogb.build.inters, v)
}

func (uogb *UserOpenidGroupBy) sqlScan(ctx context.Context, root *UserOpenidQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(uogb.fns))
	for _, fn := range uogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*uogb.flds)+len(uogb.fns))
		for _, f := range *uogb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*uogb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uogb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserOpenidSelect is the builder for selecting fields of UserOpenid entities.
type UserOpenidSelect struct {
	*UserOpenidQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (uos *UserOpenidSelect) Aggregate(fns ...AggregateFunc) *UserOpenidSelect {
	uos.fns = append(uos.fns, fns...)
	return uos
}

// Scan applies the selector query and scans the result into the given value.
func (uos *UserOpenidSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uos.ctx, "Select")
	if err := uos.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserOpenidQuery, *UserOpenidSelect](ctx, uos.UserOpenidQuery, uos, uos.inters, v)
}

func (uos *UserOpenidSelect) sqlScan(ctx context.Context, root *UserOpenidQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(uos.fns))
	for _, fn := range uos.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*uos.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uos.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
