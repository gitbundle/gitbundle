// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/gitbundle/storage/database/models/predicate"
	"github.com/gitbundle/gitbundle/storage/database/models/teamuser"
)

// TeamUserQuery is the builder for querying TeamUser entities.
type TeamUserQuery struct {
	config
	ctx        *QueryContext
	order      []teamuser.OrderOption
	inters     []Interceptor
	predicates []predicate.TeamUser
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TeamUserQuery builder.
func (tuq *TeamUserQuery) Where(ps ...predicate.TeamUser) *TeamUserQuery {
	tuq.predicates = append(tuq.predicates, ps...)
	return tuq
}

// Limit the number of records to be returned by this query.
func (tuq *TeamUserQuery) Limit(limit int) *TeamUserQuery {
	tuq.ctx.Limit = &limit
	return tuq
}

// Offset to start from.
func (tuq *TeamUserQuery) Offset(offset int) *TeamUserQuery {
	tuq.ctx.Offset = &offset
	return tuq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tuq *TeamUserQuery) Unique(unique bool) *TeamUserQuery {
	tuq.ctx.Unique = &unique
	return tuq
}

// Order specifies how the records should be ordered.
func (tuq *TeamUserQuery) Order(o ...teamuser.OrderOption) *TeamUserQuery {
	tuq.order = append(tuq.order, o...)
	return tuq
}

// First returns the first TeamUser entity from the query.
// Returns a *NotFoundError when no TeamUser was found.
func (tuq *TeamUserQuery) First(ctx context.Context) (*TeamUser, error) {
	nodes, err := tuq.Limit(1).All(setContextOp(ctx, tuq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{teamuser.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tuq *TeamUserQuery) FirstX(ctx context.Context) *TeamUser {
	node, err := tuq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TeamUser ID from the query.
// Returns a *NotFoundError when no TeamUser ID was found.
func (tuq *TeamUserQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tuq.Limit(1).IDs(setContextOp(ctx, tuq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{teamuser.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tuq *TeamUserQuery) FirstIDX(ctx context.Context) int {
	id, err := tuq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TeamUser entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TeamUser entity is found.
// Returns a *NotFoundError when no TeamUser entities are found.
func (tuq *TeamUserQuery) Only(ctx context.Context) (*TeamUser, error) {
	nodes, err := tuq.Limit(2).All(setContextOp(ctx, tuq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{teamuser.Label}
	default:
		return nil, &NotSingularError{teamuser.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tuq *TeamUserQuery) OnlyX(ctx context.Context) *TeamUser {
	node, err := tuq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TeamUser ID in the query.
// Returns a *NotSingularError when more than one TeamUser ID is found.
// Returns a *NotFoundError when no entities are found.
func (tuq *TeamUserQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tuq.Limit(2).IDs(setContextOp(ctx, tuq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{teamuser.Label}
	default:
		err = &NotSingularError{teamuser.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tuq *TeamUserQuery) OnlyIDX(ctx context.Context) int {
	id, err := tuq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TeamUsers.
func (tuq *TeamUserQuery) All(ctx context.Context) ([]*TeamUser, error) {
	ctx = setContextOp(ctx, tuq.ctx, "All")
	if err := tuq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TeamUser, *TeamUserQuery]()
	return withInterceptors[[]*TeamUser](ctx, tuq, qr, tuq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tuq *TeamUserQuery) AllX(ctx context.Context) []*TeamUser {
	nodes, err := tuq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TeamUser IDs.
func (tuq *TeamUserQuery) IDs(ctx context.Context) (ids []int, err error) {
	if tuq.ctx.Unique == nil && tuq.path != nil {
		tuq.Unique(true)
	}
	ctx = setContextOp(ctx, tuq.ctx, "IDs")
	if err = tuq.Select(teamuser.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tuq *TeamUserQuery) IDsX(ctx context.Context) []int {
	ids, err := tuq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tuq *TeamUserQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tuq.ctx, "Count")
	if err := tuq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tuq, querierCount[*TeamUserQuery](), tuq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tuq *TeamUserQuery) CountX(ctx context.Context) int {
	count, err := tuq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tuq *TeamUserQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tuq.ctx, "Exist")
	switch _, err := tuq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tuq *TeamUserQuery) ExistX(ctx context.Context) bool {
	exist, err := tuq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TeamUserQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tuq *TeamUserQuery) Clone() *TeamUserQuery {
	if tuq == nil {
		return nil
	}
	return &TeamUserQuery{
		config:     tuq.config,
		ctx:        tuq.ctx.Clone(),
		order:      append([]teamuser.OrderOption{}, tuq.order...),
		inters:     append([]Interceptor{}, tuq.inters...),
		predicates: append([]predicate.TeamUser{}, tuq.predicates...),
		// clone intermediate query.
		sql:  tuq.sql.Clone(),
		path: tuq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		OrgID int64 `json:"org_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TeamUser.Query().
//		GroupBy(teamuser.FieldOrgID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (tuq *TeamUserQuery) GroupBy(field string, fields ...string) *TeamUserGroupBy {
	tuq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TeamUserGroupBy{build: tuq}
	grbuild.flds = &tuq.ctx.Fields
	grbuild.label = teamuser.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		OrgID int64 `json:"org_id,omitempty"`
//	}
//
//	client.TeamUser.Query().
//		Select(teamuser.FieldOrgID).
//		Scan(ctx, &v)
func (tuq *TeamUserQuery) Select(fields ...string) *TeamUserSelect {
	tuq.ctx.Fields = append(tuq.ctx.Fields, fields...)
	sbuild := &TeamUserSelect{TeamUserQuery: tuq}
	sbuild.label = teamuser.Label
	sbuild.flds, sbuild.scan = &tuq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TeamUserSelect configured with the given aggregations.
func (tuq *TeamUserQuery) Aggregate(fns ...AggregateFunc) *TeamUserSelect {
	return tuq.Select().Aggregate(fns...)
}

func (tuq *TeamUserQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tuq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tuq); err != nil {
				return err
			}
		}
	}
	for _, f := range tuq.ctx.Fields {
		if !teamuser.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if tuq.path != nil {
		prev, err := tuq.path(ctx)
		if err != nil {
			return err
		}
		tuq.sql = prev
	}
	return nil
}

func (tuq *TeamUserQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TeamUser, error) {
	var (
		nodes = []*TeamUser{}
		_spec = tuq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TeamUser).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TeamUser{config: tuq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tuq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tuq *TeamUserQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tuq.querySpec()
	_spec.Node.Columns = tuq.ctx.Fields
	if len(tuq.ctx.Fields) > 0 {
		_spec.Unique = tuq.ctx.Unique != nil && *tuq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tuq.driver, _spec)
}

func (tuq *TeamUserQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(teamuser.Table, teamuser.Columns, sqlgraph.NewFieldSpec(teamuser.FieldID, field.TypeInt))
	_spec.From = tuq.sql
	if unique := tuq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tuq.path != nil {
		_spec.Unique = true
	}
	if fields := tuq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, teamuser.FieldID)
		for i := range fields {
			if fields[i] != teamuser.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tuq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tuq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tuq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tuq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tuq *TeamUserQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tuq.driver.Dialect())
	t1 := builder.Table(teamuser.Table)
	columns := tuq.ctx.Fields
	if len(columns) == 0 {
		columns = teamuser.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tuq.sql != nil {
		selector = tuq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tuq.ctx.Unique != nil && *tuq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tuq.predicates {
		p(selector)
	}
	for _, p := range tuq.order {
		p(selector)
	}
	if offset := tuq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tuq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TeamUserGroupBy is the group-by builder for TeamUser entities.
type TeamUserGroupBy struct {
	selector
	build *TeamUserQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tugb *TeamUserGroupBy) Aggregate(fns ...AggregateFunc) *TeamUserGroupBy {
	tugb.fns = append(tugb.fns, fns...)
	return tugb
}

// Scan applies the selector query and scans the result into the given value.
func (tugb *TeamUserGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tugb.build.ctx, "GroupBy")
	if err := tugb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TeamUserQuery, *TeamUserGroupBy](ctx, tugb.build, tugb, tugb.build.inters, v)
}

func (tugb *TeamUserGroupBy) sqlScan(ctx context.Context, root *TeamUserQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tugb.fns))
	for _, fn := range tugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tugb.flds)+len(tugb.fns))
		for _, f := range *tugb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tugb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tugb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TeamUserSelect is the builder for selecting fields of TeamUser entities.
type TeamUserSelect struct {
	*TeamUserQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tus *TeamUserSelect) Aggregate(fns ...AggregateFunc) *TeamUserSelect {
	tus.fns = append(tus.fns, fns...)
	return tus
}

// Scan applies the selector query and scans the result into the given value.
func (tus *TeamUserSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tus.ctx, "Select")
	if err := tus.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TeamUserQuery, *TeamUserSelect](ctx, tus.TeamUserQuery, tus, tus.inters, v)
}

func (tus *TeamUserSelect) sqlScan(ctx context.Context, root *TeamUserQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tus.fns))
	for _, fn := range tus.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tus.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
