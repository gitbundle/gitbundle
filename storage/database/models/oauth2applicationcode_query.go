// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/gitbundle/storage/database/models/oauth2applicationcode"
	"github.com/gitbundle/gitbundle/storage/database/models/predicate"
)

// Oauth2ApplicationCodeQuery is the builder for querying Oauth2ApplicationCode entities.
type Oauth2ApplicationCodeQuery struct {
	config
	ctx        *QueryContext
	order      []oauth2applicationcode.OrderOption
	inters     []Interceptor
	predicates []predicate.Oauth2ApplicationCode
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the Oauth2ApplicationCodeQuery builder.
func (ocq *Oauth2ApplicationCodeQuery) Where(ps ...predicate.Oauth2ApplicationCode) *Oauth2ApplicationCodeQuery {
	ocq.predicates = append(ocq.predicates, ps...)
	return ocq
}

// Limit the number of records to be returned by this query.
func (ocq *Oauth2ApplicationCodeQuery) Limit(limit int) *Oauth2ApplicationCodeQuery {
	ocq.ctx.Limit = &limit
	return ocq
}

// Offset to start from.
func (ocq *Oauth2ApplicationCodeQuery) Offset(offset int) *Oauth2ApplicationCodeQuery {
	ocq.ctx.Offset = &offset
	return ocq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ocq *Oauth2ApplicationCodeQuery) Unique(unique bool) *Oauth2ApplicationCodeQuery {
	ocq.ctx.Unique = &unique
	return ocq
}

// Order specifies how the records should be ordered.
func (ocq *Oauth2ApplicationCodeQuery) Order(o ...oauth2applicationcode.OrderOption) *Oauth2ApplicationCodeQuery {
	ocq.order = append(ocq.order, o...)
	return ocq
}

// First returns the first Oauth2ApplicationCode entity from the query.
// Returns a *NotFoundError when no Oauth2ApplicationCode was found.
func (ocq *Oauth2ApplicationCodeQuery) First(ctx context.Context) (*Oauth2ApplicationCode, error) {
	nodes, err := ocq.Limit(1).All(setContextOp(ctx, ocq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{oauth2applicationcode.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ocq *Oauth2ApplicationCodeQuery) FirstX(ctx context.Context) *Oauth2ApplicationCode {
	node, err := ocq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Oauth2ApplicationCode ID from the query.
// Returns a *NotFoundError when no Oauth2ApplicationCode ID was found.
func (ocq *Oauth2ApplicationCodeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ocq.Limit(1).IDs(setContextOp(ctx, ocq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{oauth2applicationcode.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ocq *Oauth2ApplicationCodeQuery) FirstIDX(ctx context.Context) int {
	id, err := ocq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Oauth2ApplicationCode entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Oauth2ApplicationCode entity is found.
// Returns a *NotFoundError when no Oauth2ApplicationCode entities are found.
func (ocq *Oauth2ApplicationCodeQuery) Only(ctx context.Context) (*Oauth2ApplicationCode, error) {
	nodes, err := ocq.Limit(2).All(setContextOp(ctx, ocq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{oauth2applicationcode.Label}
	default:
		return nil, &NotSingularError{oauth2applicationcode.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ocq *Oauth2ApplicationCodeQuery) OnlyX(ctx context.Context) *Oauth2ApplicationCode {
	node, err := ocq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Oauth2ApplicationCode ID in the query.
// Returns a *NotSingularError when more than one Oauth2ApplicationCode ID is found.
// Returns a *NotFoundError when no entities are found.
func (ocq *Oauth2ApplicationCodeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ocq.Limit(2).IDs(setContextOp(ctx, ocq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{oauth2applicationcode.Label}
	default:
		err = &NotSingularError{oauth2applicationcode.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ocq *Oauth2ApplicationCodeQuery) OnlyIDX(ctx context.Context) int {
	id, err := ocq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Oauth2ApplicationCodes.
func (ocq *Oauth2ApplicationCodeQuery) All(ctx context.Context) ([]*Oauth2ApplicationCode, error) {
	ctx = setContextOp(ctx, ocq.ctx, "All")
	if err := ocq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Oauth2ApplicationCode, *Oauth2ApplicationCodeQuery]()
	return withInterceptors[[]*Oauth2ApplicationCode](ctx, ocq, qr, ocq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ocq *Oauth2ApplicationCodeQuery) AllX(ctx context.Context) []*Oauth2ApplicationCode {
	nodes, err := ocq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Oauth2ApplicationCode IDs.
func (ocq *Oauth2ApplicationCodeQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ocq.ctx.Unique == nil && ocq.path != nil {
		ocq.Unique(true)
	}
	ctx = setContextOp(ctx, ocq.ctx, "IDs")
	if err = ocq.Select(oauth2applicationcode.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ocq *Oauth2ApplicationCodeQuery) IDsX(ctx context.Context) []int {
	ids, err := ocq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ocq *Oauth2ApplicationCodeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ocq.ctx, "Count")
	if err := ocq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ocq, querierCount[*Oauth2ApplicationCodeQuery](), ocq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ocq *Oauth2ApplicationCodeQuery) CountX(ctx context.Context) int {
	count, err := ocq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ocq *Oauth2ApplicationCodeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ocq.ctx, "Exist")
	switch _, err := ocq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ocq *Oauth2ApplicationCodeQuery) ExistX(ctx context.Context) bool {
	exist, err := ocq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the Oauth2ApplicationCodeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ocq *Oauth2ApplicationCodeQuery) Clone() *Oauth2ApplicationCodeQuery {
	if ocq == nil {
		return nil
	}
	return &Oauth2ApplicationCodeQuery{
		config:     ocq.config,
		ctx:        ocq.ctx.Clone(),
		order:      append([]oauth2applicationcode.OrderOption{}, ocq.order...),
		inters:     append([]Interceptor{}, ocq.inters...),
		predicates: append([]predicate.Oauth2ApplicationCode{}, ocq.predicates...),
		// clone intermediate query.
		sql:  ocq.sql.Clone(),
		path: ocq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		GrantID int64 `json:"grant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Oauth2ApplicationCode.Query().
//		GroupBy(oauth2applicationcode.FieldGrantID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (ocq *Oauth2ApplicationCodeQuery) GroupBy(field string, fields ...string) *Oauth2ApplicationCodeGroupBy {
	ocq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &Oauth2ApplicationCodeGroupBy{build: ocq}
	grbuild.flds = &ocq.ctx.Fields
	grbuild.label = oauth2applicationcode.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		GrantID int64 `json:"grant_id,omitempty"`
//	}
//
//	client.Oauth2ApplicationCode.Query().
//		Select(oauth2applicationcode.FieldGrantID).
//		Scan(ctx, &v)
func (ocq *Oauth2ApplicationCodeQuery) Select(fields ...string) *Oauth2ApplicationCodeSelect {
	ocq.ctx.Fields = append(ocq.ctx.Fields, fields...)
	sbuild := &Oauth2ApplicationCodeSelect{Oauth2ApplicationCodeQuery: ocq}
	sbuild.label = oauth2applicationcode.Label
	sbuild.flds, sbuild.scan = &ocq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a Oauth2ApplicationCodeSelect configured with the given aggregations.
func (ocq *Oauth2ApplicationCodeQuery) Aggregate(fns ...AggregateFunc) *Oauth2ApplicationCodeSelect {
	return ocq.Select().Aggregate(fns...)
}

func (ocq *Oauth2ApplicationCodeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ocq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ocq); err != nil {
				return err
			}
		}
	}
	for _, f := range ocq.ctx.Fields {
		if !oauth2applicationcode.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if ocq.path != nil {
		prev, err := ocq.path(ctx)
		if err != nil {
			return err
		}
		ocq.sql = prev
	}
	return nil
}

func (ocq *Oauth2ApplicationCodeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Oauth2ApplicationCode, error) {
	var (
		nodes = []*Oauth2ApplicationCode{}
		_spec = ocq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Oauth2ApplicationCode).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Oauth2ApplicationCode{config: ocq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ocq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ocq *Oauth2ApplicationCodeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ocq.querySpec()
	_spec.Node.Columns = ocq.ctx.Fields
	if len(ocq.ctx.Fields) > 0 {
		_spec.Unique = ocq.ctx.Unique != nil && *ocq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ocq.driver, _spec)
}

func (ocq *Oauth2ApplicationCodeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(oauth2applicationcode.Table, oauth2applicationcode.Columns, sqlgraph.NewFieldSpec(oauth2applicationcode.FieldID, field.TypeInt))
	_spec.From = ocq.sql
	if unique := ocq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ocq.path != nil {
		_spec.Unique = true
	}
	if fields := ocq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, oauth2applicationcode.FieldID)
		for i := range fields {
			if fields[i] != oauth2applicationcode.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ocq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ocq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ocq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ocq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ocq *Oauth2ApplicationCodeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ocq.driver.Dialect())
	t1 := builder.Table(oauth2applicationcode.Table)
	columns := ocq.ctx.Fields
	if len(columns) == 0 {
		columns = oauth2applicationcode.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ocq.sql != nil {
		selector = ocq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ocq.ctx.Unique != nil && *ocq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ocq.predicates {
		p(selector)
	}
	for _, p := range ocq.order {
		p(selector)
	}
	if offset := ocq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ocq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Oauth2ApplicationCodeGroupBy is the group-by builder for Oauth2ApplicationCode entities.
type Oauth2ApplicationCodeGroupBy struct {
	selector
	build *Oauth2ApplicationCodeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ocgb *Oauth2ApplicationCodeGroupBy) Aggregate(fns ...AggregateFunc) *Oauth2ApplicationCodeGroupBy {
	ocgb.fns = append(ocgb.fns, fns...)
	return ocgb
}

// Scan applies the selector query and scans the result into the given value.
func (ocgb *Oauth2ApplicationCodeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ocgb.build.ctx, "GroupBy")
	if err := ocgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*Oauth2ApplicationCodeQuery, *Oauth2ApplicationCodeGroupBy](ctx, ocgb.build, ocgb, ocgb.build.inters, v)
}

func (ocgb *Oauth2ApplicationCodeGroupBy) sqlScan(ctx context.Context, root *Oauth2ApplicationCodeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ocgb.fns))
	for _, fn := range ocgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ocgb.flds)+len(ocgb.fns))
		for _, f := range *ocgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ocgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ocgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Oauth2ApplicationCodeSelect is the builder for selecting fields of Oauth2ApplicationCode entities.
type Oauth2ApplicationCodeSelect struct {
	*Oauth2ApplicationCodeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ocs *Oauth2ApplicationCodeSelect) Aggregate(fns ...AggregateFunc) *Oauth2ApplicationCodeSelect {
	ocs.fns = append(ocs.fns, fns...)
	return ocs
}

// Scan applies the selector query and scans the result into the given value.
func (ocs *Oauth2ApplicationCodeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ocs.ctx, "Select")
	if err := ocs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*Oauth2ApplicationCodeQuery, *Oauth2ApplicationCodeSelect](ctx, ocs.Oauth2ApplicationCodeQuery, ocs, ocs.inters, v)
}

func (ocs *Oauth2ApplicationCodeSelect) sqlScan(ctx context.Context, root *Oauth2ApplicationCodeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ocs.fns))
	for _, fn := range ocs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ocs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ocs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
