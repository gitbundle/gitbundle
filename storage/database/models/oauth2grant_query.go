// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/gitbundle/storage/database/models/oauth2grant"
	"github.com/gitbundle/gitbundle/storage/database/models/predicate"
)

// Oauth2GrantQuery is the builder for querying Oauth2Grant entities.
type Oauth2GrantQuery struct {
	config
	ctx        *QueryContext
	order      []oauth2grant.OrderOption
	inters     []Interceptor
	predicates []predicate.Oauth2Grant
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the Oauth2GrantQuery builder.
func (oq *Oauth2GrantQuery) Where(ps ...predicate.Oauth2Grant) *Oauth2GrantQuery {
	oq.predicates = append(oq.predicates, ps...)
	return oq
}

// Limit the number of records to be returned by this query.
func (oq *Oauth2GrantQuery) Limit(limit int) *Oauth2GrantQuery {
	oq.ctx.Limit = &limit
	return oq
}

// Offset to start from.
func (oq *Oauth2GrantQuery) Offset(offset int) *Oauth2GrantQuery {
	oq.ctx.Offset = &offset
	return oq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (oq *Oauth2GrantQuery) Unique(unique bool) *Oauth2GrantQuery {
	oq.ctx.Unique = &unique
	return oq
}

// Order specifies how the records should be ordered.
func (oq *Oauth2GrantQuery) Order(o ...oauth2grant.OrderOption) *Oauth2GrantQuery {
	oq.order = append(oq.order, o...)
	return oq
}

// First returns the first Oauth2Grant entity from the query.
// Returns a *NotFoundError when no Oauth2Grant was found.
func (oq *Oauth2GrantQuery) First(ctx context.Context) (*Oauth2Grant, error) {
	nodes, err := oq.Limit(1).All(setContextOp(ctx, oq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{oauth2grant.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (oq *Oauth2GrantQuery) FirstX(ctx context.Context) *Oauth2Grant {
	node, err := oq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Oauth2Grant ID from the query.
// Returns a *NotFoundError when no Oauth2Grant ID was found.
func (oq *Oauth2GrantQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = oq.Limit(1).IDs(setContextOp(ctx, oq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{oauth2grant.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (oq *Oauth2GrantQuery) FirstIDX(ctx context.Context) int {
	id, err := oq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Oauth2Grant entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Oauth2Grant entity is found.
// Returns a *NotFoundError when no Oauth2Grant entities are found.
func (oq *Oauth2GrantQuery) Only(ctx context.Context) (*Oauth2Grant, error) {
	nodes, err := oq.Limit(2).All(setContextOp(ctx, oq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{oauth2grant.Label}
	default:
		return nil, &NotSingularError{oauth2grant.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (oq *Oauth2GrantQuery) OnlyX(ctx context.Context) *Oauth2Grant {
	node, err := oq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Oauth2Grant ID in the query.
// Returns a *NotSingularError when more than one Oauth2Grant ID is found.
// Returns a *NotFoundError when no entities are found.
func (oq *Oauth2GrantQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = oq.Limit(2).IDs(setContextOp(ctx, oq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{oauth2grant.Label}
	default:
		err = &NotSingularError{oauth2grant.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (oq *Oauth2GrantQuery) OnlyIDX(ctx context.Context) int {
	id, err := oq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Oauth2Grants.
func (oq *Oauth2GrantQuery) All(ctx context.Context) ([]*Oauth2Grant, error) {
	ctx = setContextOp(ctx, oq.ctx, "All")
	if err := oq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Oauth2Grant, *Oauth2GrantQuery]()
	return withInterceptors[[]*Oauth2Grant](ctx, oq, qr, oq.inters)
}

// AllX is like All, but panics if an error occurs.
func (oq *Oauth2GrantQuery) AllX(ctx context.Context) []*Oauth2Grant {
	nodes, err := oq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Oauth2Grant IDs.
func (oq *Oauth2GrantQuery) IDs(ctx context.Context) (ids []int, err error) {
	if oq.ctx.Unique == nil && oq.path != nil {
		oq.Unique(true)
	}
	ctx = setContextOp(ctx, oq.ctx, "IDs")
	if err = oq.Select(oauth2grant.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (oq *Oauth2GrantQuery) IDsX(ctx context.Context) []int {
	ids, err := oq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (oq *Oauth2GrantQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, oq.ctx, "Count")
	if err := oq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, oq, querierCount[*Oauth2GrantQuery](), oq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (oq *Oauth2GrantQuery) CountX(ctx context.Context) int {
	count, err := oq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (oq *Oauth2GrantQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, oq.ctx, "Exist")
	switch _, err := oq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (oq *Oauth2GrantQuery) ExistX(ctx context.Context) bool {
	exist, err := oq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the Oauth2GrantQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (oq *Oauth2GrantQuery) Clone() *Oauth2GrantQuery {
	if oq == nil {
		return nil
	}
	return &Oauth2GrantQuery{
		config:     oq.config,
		ctx:        oq.ctx.Clone(),
		order:      append([]oauth2grant.OrderOption{}, oq.order...),
		inters:     append([]Interceptor{}, oq.inters...),
		predicates: append([]predicate.Oauth2Grant{}, oq.predicates...),
		// clone intermediate query.
		sql:  oq.sql.Clone(),
		path: oq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int64 `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Oauth2Grant.Query().
//		GroupBy(oauth2grant.FieldUserID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (oq *Oauth2GrantQuery) GroupBy(field string, fields ...string) *Oauth2GrantGroupBy {
	oq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &Oauth2GrantGroupBy{build: oq}
	grbuild.flds = &oq.ctx.Fields
	grbuild.label = oauth2grant.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int64 `json:"user_id,omitempty"`
//	}
//
//	client.Oauth2Grant.Query().
//		Select(oauth2grant.FieldUserID).
//		Scan(ctx, &v)
func (oq *Oauth2GrantQuery) Select(fields ...string) *Oauth2GrantSelect {
	oq.ctx.Fields = append(oq.ctx.Fields, fields...)
	sbuild := &Oauth2GrantSelect{Oauth2GrantQuery: oq}
	sbuild.label = oauth2grant.Label
	sbuild.flds, sbuild.scan = &oq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a Oauth2GrantSelect configured with the given aggregations.
func (oq *Oauth2GrantQuery) Aggregate(fns ...AggregateFunc) *Oauth2GrantSelect {
	return oq.Select().Aggregate(fns...)
}

func (oq *Oauth2GrantQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range oq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, oq); err != nil {
				return err
			}
		}
	}
	for _, f := range oq.ctx.Fields {
		if !oauth2grant.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if oq.path != nil {
		prev, err := oq.path(ctx)
		if err != nil {
			return err
		}
		oq.sql = prev
	}
	return nil
}

func (oq *Oauth2GrantQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Oauth2Grant, error) {
	var (
		nodes = []*Oauth2Grant{}
		_spec = oq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Oauth2Grant).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Oauth2Grant{config: oq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, oq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (oq *Oauth2GrantQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := oq.querySpec()
	_spec.Node.Columns = oq.ctx.Fields
	if len(oq.ctx.Fields) > 0 {
		_spec.Unique = oq.ctx.Unique != nil && *oq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, oq.driver, _spec)
}

func (oq *Oauth2GrantQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(oauth2grant.Table, oauth2grant.Columns, sqlgraph.NewFieldSpec(oauth2grant.FieldID, field.TypeInt))
	_spec.From = oq.sql
	if unique := oq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if oq.path != nil {
		_spec.Unique = true
	}
	if fields := oq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, oauth2grant.FieldID)
		for i := range fields {
			if fields[i] != oauth2grant.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := oq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := oq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := oq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := oq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (oq *Oauth2GrantQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(oq.driver.Dialect())
	t1 := builder.Table(oauth2grant.Table)
	columns := oq.ctx.Fields
	if len(columns) == 0 {
		columns = oauth2grant.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if oq.sql != nil {
		selector = oq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if oq.ctx.Unique != nil && *oq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range oq.predicates {
		p(selector)
	}
	for _, p := range oq.order {
		p(selector)
	}
	if offset := oq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := oq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Oauth2GrantGroupBy is the group-by builder for Oauth2Grant entities.
type Oauth2GrantGroupBy struct {
	selector
	build *Oauth2GrantQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ogb *Oauth2GrantGroupBy) Aggregate(fns ...AggregateFunc) *Oauth2GrantGroupBy {
	ogb.fns = append(ogb.fns, fns...)
	return ogb
}

// Scan applies the selector query and scans the result into the given value.
func (ogb *Oauth2GrantGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ogb.build.ctx, "GroupBy")
	if err := ogb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*Oauth2GrantQuery, *Oauth2GrantGroupBy](ctx, ogb.build, ogb, ogb.build.inters, v)
}

func (ogb *Oauth2GrantGroupBy) sqlScan(ctx context.Context, root *Oauth2GrantQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ogb.fns))
	for _, fn := range ogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ogb.flds)+len(ogb.fns))
		for _, f := range *ogb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ogb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ogb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Oauth2GrantSelect is the builder for selecting fields of Oauth2Grant entities.
type Oauth2GrantSelect struct {
	*Oauth2GrantQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (os *Oauth2GrantSelect) Aggregate(fns ...AggregateFunc) *Oauth2GrantSelect {
	os.fns = append(os.fns, fns...)
	return os
}

// Scan applies the selector query and scans the result into the given value.
func (os *Oauth2GrantSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, os.ctx, "Select")
	if err := os.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*Oauth2GrantQuery, *Oauth2GrantSelect](ctx, os.Oauth2GrantQuery, os, os.inters, v)
}

func (os *Oauth2GrantSelect) sqlScan(ctx context.Context, root *Oauth2GrantQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(os.fns))
	for _, fn := range os.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*os.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := os.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
