// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/server/pkg/store/models/gpgkey"
	"github.com/gitbundle/server/pkg/store/models/predicate"
)

// GpgKeyQuery is the builder for querying GpgKey entities.
type GpgKeyQuery struct {
	config
	ctx        *QueryContext
	order      []gpgkey.OrderOption
	inters     []Interceptor
	predicates []predicate.GpgKey
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GpgKeyQuery builder.
func (gkq *GpgKeyQuery) Where(ps ...predicate.GpgKey) *GpgKeyQuery {
	gkq.predicates = append(gkq.predicates, ps...)
	return gkq
}

// Limit the number of records to be returned by this query.
func (gkq *GpgKeyQuery) Limit(limit int) *GpgKeyQuery {
	gkq.ctx.Limit = &limit
	return gkq
}

// Offset to start from.
func (gkq *GpgKeyQuery) Offset(offset int) *GpgKeyQuery {
	gkq.ctx.Offset = &offset
	return gkq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (gkq *GpgKeyQuery) Unique(unique bool) *GpgKeyQuery {
	gkq.ctx.Unique = &unique
	return gkq
}

// Order specifies how the records should be ordered.
func (gkq *GpgKeyQuery) Order(o ...gpgkey.OrderOption) *GpgKeyQuery {
	gkq.order = append(gkq.order, o...)
	return gkq
}

// First returns the first GpgKey entity from the query.
// Returns a *NotFoundError when no GpgKey was found.
func (gkq *GpgKeyQuery) First(ctx context.Context) (*GpgKey, error) {
	nodes, err := gkq.Limit(1).All(setContextOp(ctx, gkq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{gpgkey.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (gkq *GpgKeyQuery) FirstX(ctx context.Context) *GpgKey {
	node, err := gkq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GpgKey ID from the query.
// Returns a *NotFoundError when no GpgKey ID was found.
func (gkq *GpgKeyQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = gkq.Limit(1).IDs(setContextOp(ctx, gkq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{gpgkey.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (gkq *GpgKeyQuery) FirstIDX(ctx context.Context) int {
	id, err := gkq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GpgKey entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GpgKey entity is found.
// Returns a *NotFoundError when no GpgKey entities are found.
func (gkq *GpgKeyQuery) Only(ctx context.Context) (*GpgKey, error) {
	nodes, err := gkq.Limit(2).All(setContextOp(ctx, gkq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{gpgkey.Label}
	default:
		return nil, &NotSingularError{gpgkey.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (gkq *GpgKeyQuery) OnlyX(ctx context.Context) *GpgKey {
	node, err := gkq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GpgKey ID in the query.
// Returns a *NotSingularError when more than one GpgKey ID is found.
// Returns a *NotFoundError when no entities are found.
func (gkq *GpgKeyQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = gkq.Limit(2).IDs(setContextOp(ctx, gkq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{gpgkey.Label}
	default:
		err = &NotSingularError{gpgkey.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (gkq *GpgKeyQuery) OnlyIDX(ctx context.Context) int {
	id, err := gkq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GpgKeys.
func (gkq *GpgKeyQuery) All(ctx context.Context) ([]*GpgKey, error) {
	ctx = setContextOp(ctx, gkq.ctx, "All")
	if err := gkq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GpgKey, *GpgKeyQuery]()
	return withInterceptors[[]*GpgKey](ctx, gkq, qr, gkq.inters)
}

// AllX is like All, but panics if an error occurs.
func (gkq *GpgKeyQuery) AllX(ctx context.Context) []*GpgKey {
	nodes, err := gkq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GpgKey IDs.
func (gkq *GpgKeyQuery) IDs(ctx context.Context) (ids []int, err error) {
	if gkq.ctx.Unique == nil && gkq.path != nil {
		gkq.Unique(true)
	}
	ctx = setContextOp(ctx, gkq.ctx, "IDs")
	if err = gkq.Select(gpgkey.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (gkq *GpgKeyQuery) IDsX(ctx context.Context) []int {
	ids, err := gkq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (gkq *GpgKeyQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, gkq.ctx, "Count")
	if err := gkq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, gkq, querierCount[*GpgKeyQuery](), gkq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (gkq *GpgKeyQuery) CountX(ctx context.Context) int {
	count, err := gkq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (gkq *GpgKeyQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, gkq.ctx, "Exist")
	switch _, err := gkq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (gkq *GpgKeyQuery) ExistX(ctx context.Context) bool {
	exist, err := gkq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GpgKeyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (gkq *GpgKeyQuery) Clone() *GpgKeyQuery {
	if gkq == nil {
		return nil
	}
	return &GpgKeyQuery{
		config:     gkq.config,
		ctx:        gkq.ctx.Clone(),
		order:      append([]gpgkey.OrderOption{}, gkq.order...),
		inters:     append([]Interceptor{}, gkq.inters...),
		predicates: append([]predicate.GpgKey{}, gkq.predicates...),
		// clone intermediate query.
		sql:  gkq.sql.Clone(),
		path: gkq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		OwnerID int64 `json:"owner_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GpgKey.Query().
//		GroupBy(gpgkey.FieldOwnerID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (gkq *GpgKeyQuery) GroupBy(field string, fields ...string) *GpgKeyGroupBy {
	gkq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GpgKeyGroupBy{build: gkq}
	grbuild.flds = &gkq.ctx.Fields
	grbuild.label = gpgkey.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		OwnerID int64 `json:"owner_id,omitempty"`
//	}
//
//	client.GpgKey.Query().
//		Select(gpgkey.FieldOwnerID).
//		Scan(ctx, &v)
func (gkq *GpgKeyQuery) Select(fields ...string) *GpgKeySelect {
	gkq.ctx.Fields = append(gkq.ctx.Fields, fields...)
	sbuild := &GpgKeySelect{GpgKeyQuery: gkq}
	sbuild.label = gpgkey.Label
	sbuild.flds, sbuild.scan = &gkq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GpgKeySelect configured with the given aggregations.
func (gkq *GpgKeyQuery) Aggregate(fns ...AggregateFunc) *GpgKeySelect {
	return gkq.Select().Aggregate(fns...)
}

func (gkq *GpgKeyQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range gkq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, gkq); err != nil {
				return err
			}
		}
	}
	for _, f := range gkq.ctx.Fields {
		if !gpgkey.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if gkq.path != nil {
		prev, err := gkq.path(ctx)
		if err != nil {
			return err
		}
		gkq.sql = prev
	}
	return nil
}

func (gkq *GpgKeyQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GpgKey, error) {
	var (
		nodes = []*GpgKey{}
		_spec = gkq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GpgKey).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GpgKey{config: gkq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, gkq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (gkq *GpgKeyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := gkq.querySpec()
	_spec.Node.Columns = gkq.ctx.Fields
	if len(gkq.ctx.Fields) > 0 {
		_spec.Unique = gkq.ctx.Unique != nil && *gkq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, gkq.driver, _spec)
}

func (gkq *GpgKeyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(gpgkey.Table, gpgkey.Columns, sqlgraph.NewFieldSpec(gpgkey.FieldID, field.TypeInt))
	_spec.From = gkq.sql
	if unique := gkq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if gkq.path != nil {
		_spec.Unique = true
	}
	if fields := gkq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, gpgkey.FieldID)
		for i := range fields {
			if fields[i] != gpgkey.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := gkq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := gkq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := gkq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := gkq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (gkq *GpgKeyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(gkq.driver.Dialect())
	t1 := builder.Table(gpgkey.Table)
	columns := gkq.ctx.Fields
	if len(columns) == 0 {
		columns = gpgkey.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if gkq.sql != nil {
		selector = gkq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if gkq.ctx.Unique != nil && *gkq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range gkq.predicates {
		p(selector)
	}
	for _, p := range gkq.order {
		p(selector)
	}
	if offset := gkq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := gkq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// GpgKeyGroupBy is the group-by builder for GpgKey entities.
type GpgKeyGroupBy struct {
	selector
	build *GpgKeyQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gkgb *GpgKeyGroupBy) Aggregate(fns ...AggregateFunc) *GpgKeyGroupBy {
	gkgb.fns = append(gkgb.fns, fns...)
	return gkgb
}

// Scan applies the selector query and scans the result into the given value.
func (gkgb *GpgKeyGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gkgb.build.ctx, "GroupBy")
	if err := gkgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GpgKeyQuery, *GpgKeyGroupBy](ctx, gkgb.build, gkgb, gkgb.build.inters, v)
}

func (gkgb *GpgKeyGroupBy) sqlScan(ctx context.Context, root *GpgKeyQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(gkgb.fns))
	for _, fn := range gkgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*gkgb.flds)+len(gkgb.fns))
		for _, f := range *gkgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*gkgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gkgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GpgKeySelect is the builder for selecting fields of GpgKey entities.
type GpgKeySelect struct {
	*GpgKeyQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (gks *GpgKeySelect) Aggregate(fns ...AggregateFunc) *GpgKeySelect {
	gks.fns = append(gks.fns, fns...)
	return gks
}

// Scan applies the selector query and scans the result into the given value.
func (gks *GpgKeySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gks.ctx, "Select")
	if err := gks.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GpgKeyQuery, *GpgKeySelect](ctx, gks.GpgKeyQuery, gks, gks.inters, v)
}

func (gks *GpgKeySelect) sqlScan(ctx context.Context, root *GpgKeyQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(gks.fns))
	for _, fn := range gks.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*gks.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gks.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
