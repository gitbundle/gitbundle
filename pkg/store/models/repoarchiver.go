// Code generated by ent, DO NOT EDIT.

package models

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/gitbundle/server/pkg/store/models/repoarchiver"
)

// RepoArchiver is the model entity for the RepoArchiver schema.
type RepoArchiver struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// RepoID holds the value of the "repo_id" field.
	RepoID int64 `json:"repo_id,omitempty"`
	// Type holds the value of the "type" field.
	Type int `json:"type,omitempty"`
	// Status holds the value of the "status" field.
	Status int `json:"status,omitempty"`
	// CommitID holds the value of the "commit_id" field.
	CommitID string `json:"commit_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RepoArchiver) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case repoarchiver.FieldID, repoarchiver.FieldRepoID, repoarchiver.FieldType, repoarchiver.FieldStatus:
			values[i] = new(sql.NullInt64)
		case repoarchiver.FieldCommitID:
			values[i] = new(sql.NullString)
		case repoarchiver.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RepoArchiver fields.
func (ra *RepoArchiver) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case repoarchiver.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ra.ID = int(value.Int64)
		case repoarchiver.FieldRepoID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field repo_id", values[i])
			} else if value.Valid {
				ra.RepoID = value.Int64
			}
		case repoarchiver.FieldType:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				ra.Type = int(value.Int64)
			}
		case repoarchiver.FieldStatus:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				ra.Status = int(value.Int64)
			}
		case repoarchiver.FieldCommitID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field commit_id", values[i])
			} else if value.Valid {
				ra.CommitID = value.String
			}
		case repoarchiver.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ra.CreatedAt = value.Time
			}
		default:
			ra.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RepoArchiver.
// This includes values selected through modifiers, order, etc.
func (ra *RepoArchiver) Value(name string) (ent.Value, error) {
	return ra.selectValues.Get(name)
}

// Update returns a builder for updating this RepoArchiver.
// Note that you need to call RepoArchiver.Unwrap() before calling this method if this RepoArchiver
// was returned from a transaction, and the transaction was committed or rolled back.
func (ra *RepoArchiver) Update() *RepoArchiverUpdateOne {
	return NewRepoArchiverClient(ra.config).UpdateOne(ra)
}

// Unwrap unwraps the RepoArchiver entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ra *RepoArchiver) Unwrap() *RepoArchiver {
	_tx, ok := ra.config.driver.(*txDriver)
	if !ok {
		panic("models: RepoArchiver is not a transactional entity")
	}
	ra.config.driver = _tx.drv
	return ra
}

// String implements the fmt.Stringer.
func (ra *RepoArchiver) String() string {
	var builder strings.Builder
	builder.WriteString("RepoArchiver(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ra.ID))
	builder.WriteString("repo_id=")
	builder.WriteString(fmt.Sprintf("%v", ra.RepoID))
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", ra.Type))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", ra.Status))
	builder.WriteString(", ")
	builder.WriteString("commit_id=")
	builder.WriteString(ra.CommitID)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ra.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RepoArchivers is a parsable slice of RepoArchiver.
type RepoArchivers []*RepoArchiver
