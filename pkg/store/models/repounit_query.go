// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/server/pkg/store/models/predicate"
	"github.com/gitbundle/server/pkg/store/models/repounit"
)

// RepoUnitQuery is the builder for querying RepoUnit entities.
type RepoUnitQuery struct {
	config
	ctx        *QueryContext
	order      []repounit.OrderOption
	inters     []Interceptor
	predicates []predicate.RepoUnit
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RepoUnitQuery builder.
func (ruq *RepoUnitQuery) Where(ps ...predicate.RepoUnit) *RepoUnitQuery {
	ruq.predicates = append(ruq.predicates, ps...)
	return ruq
}

// Limit the number of records to be returned by this query.
func (ruq *RepoUnitQuery) Limit(limit int) *RepoUnitQuery {
	ruq.ctx.Limit = &limit
	return ruq
}

// Offset to start from.
func (ruq *RepoUnitQuery) Offset(offset int) *RepoUnitQuery {
	ruq.ctx.Offset = &offset
	return ruq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ruq *RepoUnitQuery) Unique(unique bool) *RepoUnitQuery {
	ruq.ctx.Unique = &unique
	return ruq
}

// Order specifies how the records should be ordered.
func (ruq *RepoUnitQuery) Order(o ...repounit.OrderOption) *RepoUnitQuery {
	ruq.order = append(ruq.order, o...)
	return ruq
}

// First returns the first RepoUnit entity from the query.
// Returns a *NotFoundError when no RepoUnit was found.
func (ruq *RepoUnitQuery) First(ctx context.Context) (*RepoUnit, error) {
	nodes, err := ruq.Limit(1).All(setContextOp(ctx, ruq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{repounit.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ruq *RepoUnitQuery) FirstX(ctx context.Context) *RepoUnit {
	node, err := ruq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RepoUnit ID from the query.
// Returns a *NotFoundError when no RepoUnit ID was found.
func (ruq *RepoUnitQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ruq.Limit(1).IDs(setContextOp(ctx, ruq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{repounit.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ruq *RepoUnitQuery) FirstIDX(ctx context.Context) int {
	id, err := ruq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RepoUnit entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RepoUnit entity is found.
// Returns a *NotFoundError when no RepoUnit entities are found.
func (ruq *RepoUnitQuery) Only(ctx context.Context) (*RepoUnit, error) {
	nodes, err := ruq.Limit(2).All(setContextOp(ctx, ruq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{repounit.Label}
	default:
		return nil, &NotSingularError{repounit.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ruq *RepoUnitQuery) OnlyX(ctx context.Context) *RepoUnit {
	node, err := ruq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RepoUnit ID in the query.
// Returns a *NotSingularError when more than one RepoUnit ID is found.
// Returns a *NotFoundError when no entities are found.
func (ruq *RepoUnitQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ruq.Limit(2).IDs(setContextOp(ctx, ruq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{repounit.Label}
	default:
		err = &NotSingularError{repounit.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ruq *RepoUnitQuery) OnlyIDX(ctx context.Context) int {
	id, err := ruq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RepoUnits.
func (ruq *RepoUnitQuery) All(ctx context.Context) ([]*RepoUnit, error) {
	ctx = setContextOp(ctx, ruq.ctx, "All")
	if err := ruq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RepoUnit, *RepoUnitQuery]()
	return withInterceptors[[]*RepoUnit](ctx, ruq, qr, ruq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ruq *RepoUnitQuery) AllX(ctx context.Context) []*RepoUnit {
	nodes, err := ruq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RepoUnit IDs.
func (ruq *RepoUnitQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ruq.ctx.Unique == nil && ruq.path != nil {
		ruq.Unique(true)
	}
	ctx = setContextOp(ctx, ruq.ctx, "IDs")
	if err = ruq.Select(repounit.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ruq *RepoUnitQuery) IDsX(ctx context.Context) []int {
	ids, err := ruq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ruq *RepoUnitQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ruq.ctx, "Count")
	if err := ruq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ruq, querierCount[*RepoUnitQuery](), ruq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ruq *RepoUnitQuery) CountX(ctx context.Context) int {
	count, err := ruq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ruq *RepoUnitQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ruq.ctx, "Exist")
	switch _, err := ruq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ruq *RepoUnitQuery) ExistX(ctx context.Context) bool {
	exist, err := ruq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RepoUnitQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ruq *RepoUnitQuery) Clone() *RepoUnitQuery {
	if ruq == nil {
		return nil
	}
	return &RepoUnitQuery{
		config:     ruq.config,
		ctx:        ruq.ctx.Clone(),
		order:      append([]repounit.OrderOption{}, ruq.order...),
		inters:     append([]Interceptor{}, ruq.inters...),
		predicates: append([]predicate.RepoUnit{}, ruq.predicates...),
		// clone intermediate query.
		sql:  ruq.sql.Clone(),
		path: ruq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RepoID int64 `json:"repo_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RepoUnit.Query().
//		GroupBy(repounit.FieldRepoID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (ruq *RepoUnitQuery) GroupBy(field string, fields ...string) *RepoUnitGroupBy {
	ruq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RepoUnitGroupBy{build: ruq}
	grbuild.flds = &ruq.ctx.Fields
	grbuild.label = repounit.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RepoID int64 `json:"repo_id,omitempty"`
//	}
//
//	client.RepoUnit.Query().
//		Select(repounit.FieldRepoID).
//		Scan(ctx, &v)
func (ruq *RepoUnitQuery) Select(fields ...string) *RepoUnitSelect {
	ruq.ctx.Fields = append(ruq.ctx.Fields, fields...)
	sbuild := &RepoUnitSelect{RepoUnitQuery: ruq}
	sbuild.label = repounit.Label
	sbuild.flds, sbuild.scan = &ruq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RepoUnitSelect configured with the given aggregations.
func (ruq *RepoUnitQuery) Aggregate(fns ...AggregateFunc) *RepoUnitSelect {
	return ruq.Select().Aggregate(fns...)
}

func (ruq *RepoUnitQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ruq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ruq); err != nil {
				return err
			}
		}
	}
	for _, f := range ruq.ctx.Fields {
		if !repounit.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if ruq.path != nil {
		prev, err := ruq.path(ctx)
		if err != nil {
			return err
		}
		ruq.sql = prev
	}
	return nil
}

func (ruq *RepoUnitQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RepoUnit, error) {
	var (
		nodes = []*RepoUnit{}
		_spec = ruq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RepoUnit).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RepoUnit{config: ruq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ruq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ruq *RepoUnitQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ruq.querySpec()
	_spec.Node.Columns = ruq.ctx.Fields
	if len(ruq.ctx.Fields) > 0 {
		_spec.Unique = ruq.ctx.Unique != nil && *ruq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ruq.driver, _spec)
}

func (ruq *RepoUnitQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(repounit.Table, repounit.Columns, sqlgraph.NewFieldSpec(repounit.FieldID, field.TypeInt))
	_spec.From = ruq.sql
	if unique := ruq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ruq.path != nil {
		_spec.Unique = true
	}
	if fields := ruq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, repounit.FieldID)
		for i := range fields {
			if fields[i] != repounit.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ruq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ruq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ruq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ruq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ruq *RepoUnitQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ruq.driver.Dialect())
	t1 := builder.Table(repounit.Table)
	columns := ruq.ctx.Fields
	if len(columns) == 0 {
		columns = repounit.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ruq.sql != nil {
		selector = ruq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ruq.ctx.Unique != nil && *ruq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ruq.predicates {
		p(selector)
	}
	for _, p := range ruq.order {
		p(selector)
	}
	if offset := ruq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ruq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RepoUnitGroupBy is the group-by builder for RepoUnit entities.
type RepoUnitGroupBy struct {
	selector
	build *RepoUnitQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rugb *RepoUnitGroupBy) Aggregate(fns ...AggregateFunc) *RepoUnitGroupBy {
	rugb.fns = append(rugb.fns, fns...)
	return rugb
}

// Scan applies the selector query and scans the result into the given value.
func (rugb *RepoUnitGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rugb.build.ctx, "GroupBy")
	if err := rugb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RepoUnitQuery, *RepoUnitGroupBy](ctx, rugb.build, rugb, rugb.build.inters, v)
}

func (rugb *RepoUnitGroupBy) sqlScan(ctx context.Context, root *RepoUnitQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rugb.fns))
	for _, fn := range rugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rugb.flds)+len(rugb.fns))
		for _, f := range *rugb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rugb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rugb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RepoUnitSelect is the builder for selecting fields of RepoUnit entities.
type RepoUnitSelect struct {
	*RepoUnitQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rus *RepoUnitSelect) Aggregate(fns ...AggregateFunc) *RepoUnitSelect {
	rus.fns = append(rus.fns, fns...)
	return rus
}

// Scan applies the selector query and scans the result into the given value.
func (rus *RepoUnitSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rus.ctx, "Select")
	if err := rus.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RepoUnitQuery, *RepoUnitSelect](ctx, rus.RepoUnitQuery, rus, rus.inters, v)
}

func (rus *RepoUnitSelect) sqlScan(ctx context.Context, root *RepoUnitQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rus.fns))
	for _, fn := range rus.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rus.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
