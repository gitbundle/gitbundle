// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gitbundle/server/pkg/store/models/emailhash"
	"github.com/gitbundle/server/pkg/store/models/predicate"
)

// EmailHashQuery is the builder for querying EmailHash entities.
type EmailHashQuery struct {
	config
	ctx        *QueryContext
	order      []emailhash.OrderOption
	inters     []Interceptor
	predicates []predicate.EmailHash
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EmailHashQuery builder.
func (ehq *EmailHashQuery) Where(ps ...predicate.EmailHash) *EmailHashQuery {
	ehq.predicates = append(ehq.predicates, ps...)
	return ehq
}

// Limit the number of records to be returned by this query.
func (ehq *EmailHashQuery) Limit(limit int) *EmailHashQuery {
	ehq.ctx.Limit = &limit
	return ehq
}

// Offset to start from.
func (ehq *EmailHashQuery) Offset(offset int) *EmailHashQuery {
	ehq.ctx.Offset = &offset
	return ehq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ehq *EmailHashQuery) Unique(unique bool) *EmailHashQuery {
	ehq.ctx.Unique = &unique
	return ehq
}

// Order specifies how the records should be ordered.
func (ehq *EmailHashQuery) Order(o ...emailhash.OrderOption) *EmailHashQuery {
	ehq.order = append(ehq.order, o...)
	return ehq
}

// First returns the first EmailHash entity from the query.
// Returns a *NotFoundError when no EmailHash was found.
func (ehq *EmailHashQuery) First(ctx context.Context) (*EmailHash, error) {
	nodes, err := ehq.Limit(1).All(setContextOp(ctx, ehq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{emailhash.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ehq *EmailHashQuery) FirstX(ctx context.Context) *EmailHash {
	node, err := ehq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EmailHash ID from the query.
// Returns a *NotFoundError when no EmailHash ID was found.
func (ehq *EmailHashQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ehq.Limit(1).IDs(setContextOp(ctx, ehq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{emailhash.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ehq *EmailHashQuery) FirstIDX(ctx context.Context) int {
	id, err := ehq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EmailHash entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EmailHash entity is found.
// Returns a *NotFoundError when no EmailHash entities are found.
func (ehq *EmailHashQuery) Only(ctx context.Context) (*EmailHash, error) {
	nodes, err := ehq.Limit(2).All(setContextOp(ctx, ehq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{emailhash.Label}
	default:
		return nil, &NotSingularError{emailhash.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ehq *EmailHashQuery) OnlyX(ctx context.Context) *EmailHash {
	node, err := ehq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EmailHash ID in the query.
// Returns a *NotSingularError when more than one EmailHash ID is found.
// Returns a *NotFoundError when no entities are found.
func (ehq *EmailHashQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ehq.Limit(2).IDs(setContextOp(ctx, ehq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{emailhash.Label}
	default:
		err = &NotSingularError{emailhash.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ehq *EmailHashQuery) OnlyIDX(ctx context.Context) int {
	id, err := ehq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EmailHashes.
func (ehq *EmailHashQuery) All(ctx context.Context) ([]*EmailHash, error) {
	ctx = setContextOp(ctx, ehq.ctx, "All")
	if err := ehq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EmailHash, *EmailHashQuery]()
	return withInterceptors[[]*EmailHash](ctx, ehq, qr, ehq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ehq *EmailHashQuery) AllX(ctx context.Context) []*EmailHash {
	nodes, err := ehq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EmailHash IDs.
func (ehq *EmailHashQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ehq.ctx.Unique == nil && ehq.path != nil {
		ehq.Unique(true)
	}
	ctx = setContextOp(ctx, ehq.ctx, "IDs")
	if err = ehq.Select(emailhash.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ehq *EmailHashQuery) IDsX(ctx context.Context) []int {
	ids, err := ehq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ehq *EmailHashQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ehq.ctx, "Count")
	if err := ehq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ehq, querierCount[*EmailHashQuery](), ehq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ehq *EmailHashQuery) CountX(ctx context.Context) int {
	count, err := ehq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ehq *EmailHashQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ehq.ctx, "Exist")
	switch _, err := ehq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ehq *EmailHashQuery) ExistX(ctx context.Context) bool {
	exist, err := ehq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EmailHashQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ehq *EmailHashQuery) Clone() *EmailHashQuery {
	if ehq == nil {
		return nil
	}
	return &EmailHashQuery{
		config:     ehq.config,
		ctx:        ehq.ctx.Clone(),
		order:      append([]emailhash.OrderOption{}, ehq.order...),
		inters:     append([]Interceptor{}, ehq.inters...),
		predicates: append([]predicate.EmailHash{}, ehq.predicates...),
		// clone intermediate query.
		sql:  ehq.sql.Clone(),
		path: ehq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Hash string `json:"hash,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EmailHash.Query().
//		GroupBy(emailhash.FieldHash).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (ehq *EmailHashQuery) GroupBy(field string, fields ...string) *EmailHashGroupBy {
	ehq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EmailHashGroupBy{build: ehq}
	grbuild.flds = &ehq.ctx.Fields
	grbuild.label = emailhash.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Hash string `json:"hash,omitempty"`
//	}
//
//	client.EmailHash.Query().
//		Select(emailhash.FieldHash).
//		Scan(ctx, &v)
func (ehq *EmailHashQuery) Select(fields ...string) *EmailHashSelect {
	ehq.ctx.Fields = append(ehq.ctx.Fields, fields...)
	sbuild := &EmailHashSelect{EmailHashQuery: ehq}
	sbuild.label = emailhash.Label
	sbuild.flds, sbuild.scan = &ehq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EmailHashSelect configured with the given aggregations.
func (ehq *EmailHashQuery) Aggregate(fns ...AggregateFunc) *EmailHashSelect {
	return ehq.Select().Aggregate(fns...)
}

func (ehq *EmailHashQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ehq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ehq); err != nil {
				return err
			}
		}
	}
	for _, f := range ehq.ctx.Fields {
		if !emailhash.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if ehq.path != nil {
		prev, err := ehq.path(ctx)
		if err != nil {
			return err
		}
		ehq.sql = prev
	}
	return nil
}

func (ehq *EmailHashQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EmailHash, error) {
	var (
		nodes = []*EmailHash{}
		_spec = ehq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EmailHash).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EmailHash{config: ehq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ehq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ehq *EmailHashQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ehq.querySpec()
	_spec.Node.Columns = ehq.ctx.Fields
	if len(ehq.ctx.Fields) > 0 {
		_spec.Unique = ehq.ctx.Unique != nil && *ehq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ehq.driver, _spec)
}

func (ehq *EmailHashQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(emailhash.Table, emailhash.Columns, sqlgraph.NewFieldSpec(emailhash.FieldID, field.TypeInt))
	_spec.From = ehq.sql
	if unique := ehq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ehq.path != nil {
		_spec.Unique = true
	}
	if fields := ehq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, emailhash.FieldID)
		for i := range fields {
			if fields[i] != emailhash.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ehq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ehq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ehq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ehq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ehq *EmailHashQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ehq.driver.Dialect())
	t1 := builder.Table(emailhash.Table)
	columns := ehq.ctx.Fields
	if len(columns) == 0 {
		columns = emailhash.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ehq.sql != nil {
		selector = ehq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ehq.ctx.Unique != nil && *ehq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ehq.predicates {
		p(selector)
	}
	for _, p := range ehq.order {
		p(selector)
	}
	if offset := ehq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ehq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EmailHashGroupBy is the group-by builder for EmailHash entities.
type EmailHashGroupBy struct {
	selector
	build *EmailHashQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ehgb *EmailHashGroupBy) Aggregate(fns ...AggregateFunc) *EmailHashGroupBy {
	ehgb.fns = append(ehgb.fns, fns...)
	return ehgb
}

// Scan applies the selector query and scans the result into the given value.
func (ehgb *EmailHashGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ehgb.build.ctx, "GroupBy")
	if err := ehgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EmailHashQuery, *EmailHashGroupBy](ctx, ehgb.build, ehgb, ehgb.build.inters, v)
}

func (ehgb *EmailHashGroupBy) sqlScan(ctx context.Context, root *EmailHashQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ehgb.fns))
	for _, fn := range ehgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ehgb.flds)+len(ehgb.fns))
		for _, f := range *ehgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ehgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ehgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EmailHashSelect is the builder for selecting fields of EmailHash entities.
type EmailHashSelect struct {
	*EmailHashQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ehs *EmailHashSelect) Aggregate(fns ...AggregateFunc) *EmailHashSelect {
	ehs.fns = append(ehs.fns, fns...)
	return ehs
}

// Scan applies the selector query and scans the result into the given value.
func (ehs *EmailHashSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ehs.ctx, "Select")
	if err := ehs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EmailHashQuery, *EmailHashSelect](ctx, ehs.EmailHashQuery, ehs, ehs.inters, v)
}

func (ehs *EmailHashSelect) sqlScan(ctx context.Context, root *EmailHashQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ehs.fns))
	for _, fn := range ehs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ehs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ehs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
