// Code generated by ent, DO NOT EDIT.

package models

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/gitbundle/server/pkg/store/models/teamrepo"
)

// TeamRepo is the model entity for the TeamRepo schema.
type TeamRepo struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// OrgID holds the value of the "org_id" field.
	OrgID int64 `json:"org_id,omitempty"`
	// TeamID holds the value of the "team_id" field.
	TeamID int64 `json:"team_id,omitempty"`
	// RepoID holds the value of the "repo_id" field.
	RepoID       int64 `json:"repo_id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TeamRepo) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case teamrepo.FieldID, teamrepo.FieldOrgID, teamrepo.FieldTeamID, teamrepo.FieldRepoID:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TeamRepo fields.
func (tr *TeamRepo) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case teamrepo.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			tr.ID = int(value.Int64)
		case teamrepo.FieldOrgID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field org_id", values[i])
			} else if value.Valid {
				tr.OrgID = value.Int64
			}
		case teamrepo.FieldTeamID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field team_id", values[i])
			} else if value.Valid {
				tr.TeamID = value.Int64
			}
		case teamrepo.FieldRepoID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field repo_id", values[i])
			} else if value.Valid {
				tr.RepoID = value.Int64
			}
		default:
			tr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TeamRepo.
// This includes values selected through modifiers, order, etc.
func (tr *TeamRepo) Value(name string) (ent.Value, error) {
	return tr.selectValues.Get(name)
}

// Update returns a builder for updating this TeamRepo.
// Note that you need to call TeamRepo.Unwrap() before calling this method if this TeamRepo
// was returned from a transaction, and the transaction was committed or rolled back.
func (tr *TeamRepo) Update() *TeamRepoUpdateOne {
	return NewTeamRepoClient(tr.config).UpdateOne(tr)
}

// Unwrap unwraps the TeamRepo entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tr *TeamRepo) Unwrap() *TeamRepo {
	_tx, ok := tr.config.driver.(*txDriver)
	if !ok {
		panic("models: TeamRepo is not a transactional entity")
	}
	tr.config.driver = _tx.drv
	return tr
}

// String implements the fmt.Stringer.
func (tr *TeamRepo) String() string {
	var builder strings.Builder
	builder.WriteString("TeamRepo(")
	builder.WriteString(fmt.Sprintf("id=%v, ", tr.ID))
	builder.WriteString("org_id=")
	builder.WriteString(fmt.Sprintf("%v", tr.OrgID))
	builder.WriteString(", ")
	builder.WriteString("team_id=")
	builder.WriteString(fmt.Sprintf("%v", tr.TeamID))
	builder.WriteString(", ")
	builder.WriteString("repo_id=")
	builder.WriteString(fmt.Sprintf("%v", tr.RepoID))
	builder.WriteByte(')')
	return builder.String()
}

// TeamRepos is a parsable slice of TeamRepo.
type TeamRepos []*TeamRepo
