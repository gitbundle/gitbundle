// Code generated by ent, DO NOT EDIT.

package models

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/gitbundle/server/pkg/store/models/commitstatusindex"
)

// CommitStatusIndex is the model entity for the CommitStatusIndex schema.
type CommitStatusIndex struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// RepoID holds the value of the "repo_id" field.
	RepoID int64 `json:"repo_id,omitempty"`
	// Sha holds the value of the "sha" field.
	Sha string `json:"sha,omitempty"`
	// MaxIndex holds the value of the "max_index" field.
	MaxIndex     int64 `json:"max_index,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CommitStatusIndex) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case commitstatusindex.FieldID, commitstatusindex.FieldRepoID, commitstatusindex.FieldMaxIndex:
			values[i] = new(sql.NullInt64)
		case commitstatusindex.FieldSha:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CommitStatusIndex fields.
func (csi *CommitStatusIndex) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case commitstatusindex.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			csi.ID = int(value.Int64)
		case commitstatusindex.FieldRepoID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field repo_id", values[i])
			} else if value.Valid {
				csi.RepoID = value.Int64
			}
		case commitstatusindex.FieldSha:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field sha", values[i])
			} else if value.Valid {
				csi.Sha = value.String
			}
		case commitstatusindex.FieldMaxIndex:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field max_index", values[i])
			} else if value.Valid {
				csi.MaxIndex = value.Int64
			}
		default:
			csi.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CommitStatusIndex.
// This includes values selected through modifiers, order, etc.
func (csi *CommitStatusIndex) Value(name string) (ent.Value, error) {
	return csi.selectValues.Get(name)
}

// Update returns a builder for updating this CommitStatusIndex.
// Note that you need to call CommitStatusIndex.Unwrap() before calling this method if this CommitStatusIndex
// was returned from a transaction, and the transaction was committed or rolled back.
func (csi *CommitStatusIndex) Update() *CommitStatusIndexUpdateOne {
	return NewCommitStatusIndexClient(csi.config).UpdateOne(csi)
}

// Unwrap unwraps the CommitStatusIndex entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (csi *CommitStatusIndex) Unwrap() *CommitStatusIndex {
	_tx, ok := csi.config.driver.(*txDriver)
	if !ok {
		panic("models: CommitStatusIndex is not a transactional entity")
	}
	csi.config.driver = _tx.drv
	return csi
}

// String implements the fmt.Stringer.
func (csi *CommitStatusIndex) String() string {
	var builder strings.Builder
	builder.WriteString("CommitStatusIndex(")
	builder.WriteString(fmt.Sprintf("id=%v, ", csi.ID))
	builder.WriteString("repo_id=")
	builder.WriteString(fmt.Sprintf("%v", csi.RepoID))
	builder.WriteString(", ")
	builder.WriteString("sha=")
	builder.WriteString(csi.Sha)
	builder.WriteString(", ")
	builder.WriteString("max_index=")
	builder.WriteString(fmt.Sprintf("%v", csi.MaxIndex))
	builder.WriteByte(')')
	return builder.String()
}

// CommitStatusIndexes is a parsable slice of CommitStatusIndex.
type CommitStatusIndexes []*CommitStatusIndex
